# 볶음밥 (Bokk) 프로젝트 컨텍스트

이 문서는 볶음밥(Bokk) 프로젝트의 구조와 기술 스택에 대한 핵심 정보를 담고 있으며, AI 에이전트가 코드를 작성하거나 수정할 때 기준이 되는 컨텍스트입니다.

## 1. 프로젝트 개요
**'볶음밥(Bokk)'**은 사용자가 자주 사용하는 기능들을 한곳에 모아둔 다목적 유틸리티 웹 어플리케이션입니다. 주요 포털 검색, 즐겨찾기(웹 링크) 관리, 유튜브 재생 목록 관리 및 백그라운드 재생, 게시판 등 개인화된 편의 기능을 제공합니다.

- **홈페이지:** [https://bokk.jiokkae.com/](https://bokk.jiokkae.com/)
- **주요 목적:** 접근성이 높은 웹 기반 개인 대시보드 및 편의 기능 모음

## 2. 사용된 기술 스택 (Tech Stack)

### Frontend Core
- **React (v18.2.0):** UI 컴포넌트 기반 렌더링
- **React Router DOM (v6):** SPA 라우팅 처리 (`BrowserRouter`, `Routes`, `Outlet` 활용)

### 상태 관리 & API 통신
- **Apollo Client (`@apollo/client` v3):** GraphQL 기반 서버 통신 및 로컬 캐싱 관리
- **GraphQL:** 서버와의 데이터 질의 및 조작

### 스타일링 & UI 라이브러리
- **CSS Modules (`*.module.css`) & App.css:** 기본 컴포넌트 스타일링
- **Styled Components:** CSS-in-JS 스타일링
- **React Bootstrap:** 주요 UI 프레임워크

### 주요 기능 라이브러리
- **React Youtube:** 유튜브 영상 플레이어 내장 및 제어
- **CKEditor 5 (`@ckeditor/ckeditor5-react`):** 리치 텍스트 에디터

## 3. 핵심 아키텍처 및 작업 규칙

1. **라우팅:**
   - `App.js`에서 `react-router-dom`의 Outlet 기능을 활용하여 레이아웃(`HeaderLayout.js`, `SignLayout.js`) 기반으로 라우팅을 구성합니다.
2. **API 통신:**
   - 모든 서버 통신은 `Apollo Client`와 `GraphQL`을 사용합니다.
   - 쿼리와 뮤테이션은 `src/constants/querys.js`에 중앙 집중화되어 있습니다.
3. **접근 제어:**
   - 컴포넌트 내에서 `ME` 쿼리(GraphQL)를 사용하여 로그인 상태를 확인하고 UI 렌더링 및 기능 접근을 제어합니다.
4. **스타일링 컨벤션:**
   - 컴포넌트 스코프 스타일링이 필요한 경우 CSS Modules (`[ComponentName].module.css`)를 우선적으로 사용합니다.
5. **커밋 및 배포 규칙 (중요):**
   - 코드 수정 작업 후 AI가 임의로 커밋(`git commit`, `git push`)하거나 배포(`npm run deploy`)를 실행하지 않으며, 반드시 **사용자가 명시적으로 커밋/배포를 지시할 때만** 수행합니다.

## 4. 주요 디렉토리 구조
- `src/components/`: 재사용 가능한 UI 컴포넌트
- `src/routers/`: 각 라우트(페이지)에 해당하는 최상위 컴포넌트
- `src/outlets/`: 중첩 라우팅을 위한 레이아웃 컴포넌트
- `src/constants/`: GraphQL 쿼리(`querys.js`), 프래그먼트, URL 등 전역 상수

## 5. 주요 신규 기능 및 업데이트 이력

### 5.1 기능 추가 내역 모달 (`FeatureHistoryModal.js`)
- **위치**: 헤더 검색 선택 영역 마인크래프트 아이콘 왼쪽 (`SearchSelector.js`)
- **아코디언 폴딩 구조**: 메인 기능 카테고리별 독립적 Fold/Unfold 애니메이션 모달 UI
- **최신 갱신 자동 정렬**: 카테고리 내부 소기능 중 가장 최근 업데이트 날짜가 있는 카테고리가 최상단에 자동 배치
- **UI & 스크롤바 디테일**: 접힌 카테고리 딤드 배경(`#f1f3f5`), 슬림 고대비 항시 표시 스크롤바(`overflowY: scroll`), 간결한 단문 설명 및 닫기 버튼 없는 푸터 레이아웃

### 5.2 내 웹링크 드래그 앤 드롭 정렬 (`MyWeblink.js`)
- **내 웹링크 편집 화면**: 삼단줄(`☰`) 잡기 기반 실시간 드래그 앤 드롭 순서 변경 및 백엔드 DB 자동 연동

### 5.3 유튜브 재생 실시간 스크롤 티커 (`YoutubeOffcanvas.js`)
- **위치**: 헤더 유튜브 아이콘 왼쪽
- **내용**: 유튜브 영상 재생 시 현재 재생 중인 영상 제목이 펼쳐짐/접힘 애니메이션과 함께 가로 무한 스크롤되는 라이브 티커 제공

### 5.4 웹링크 추가 위치 지정 옵션 (`AddWeblinkModal.js`)
- **position 옵션**: GraphQL Mutation `addWeblink` 호출 시 `position` (`first` / `last`)을 지정하여 추가 위치 선택
- **관리 페이지**: 상단 `+` 버튼으로 추가 시 목록 맨 위(`position: "first"`)에 자동 배치
- **메인 페이지**: `+` 버튼으로 추가 시 목록 맨 끝(`position: "last"`)에 자동 배치

### 5.5 모바일 한글 IME 모아쓰기 호환 에디터 구축 (`PostMessageForm.js`)
- **버그 해결**: 모바일 환경(삼성 키보드 천지인 등)에서 CKEditor 5 가상 DOM 노드 조작으로 발생하던 한글 자모 분리 현상(`아  ㅇ  ㅏ`) 완전 제거
- **IME-Safe 에디터**: 브라우저 네이티브 한글 조합(`compositionstart/end`) 엔진 기반 리치 텍스트 에디터 구축
- **기능 및 성능**: 굵게, 링크, 모바일 이미지 자동 압축 업로드, 유튜브 비디오 임베드, 실행 취소/다시 실행 지원 및 번들 용량 절감 (-250KB)
- **모바일 이미지 EXIF 회전 버그 수정**: 모바일에서 세로로 찍은 사진 업로드 시 가로로 눕는 현상을 해결하기 위해 `createImageBitmap(file, { imageOrientation: "from-image" })`를 도입하여 메타데이터 기반 자동 정방향 회전 적용
- **IMESafeEditor 무한 렌더링 루프 에러 수정**: `useEffect` 의존성 배열에 포함된 `onReady` 콜백 함수가 부모 컴포넌트(`PostMessageForm`) 렌더링 시마다 재생성되며 무한 렌더링(`Maximum update depth exceeded`)을 유발하는 문제를 `useCallback` 메모이제이션을 통해 해결

### 5.6 모바일 이미지 업로드 UX 및 메모리/포커스 버그 전면 개선 (`PostMessageForm.js`)
- **업로드 체감 속도 개선 (Optimistic UI)**: 이미지 선택 즉시 로컬 미리보기(Blob URL)를 반투명하게 에디터에 삽입하고 50ms 대기 후 압축/업로드를 시작하여, 메인 스레드 블로킹으로 인한 화면 멈춤 현상 해결.
- **모바일 에디터 포커스 유실 버그 수정**: 파일 선택창(갤러리)에서 복귀 시 커서 포커스를 잃어 미리보기가 삽입되지 않는 모바일 브라우저 버그를 `insertAdjacentHTML` Fallback과 `focus()`로 완벽 해결.
- **모바일 브라우저 OOM 및 재선택 버그 해결**: 연속 업로드 시 메모리 부족으로 인한 뻗음 현상을 막기 위해 캔버스 드로잉 직후 `imgBitmap.close()`로 기기 RAM 즉각 해제. 또한 파일 재선택 불가 버그는 `<input type="file">`의 React `key`를 갱신해 엘리먼트를 매번 재생성하는 방식으로 완벽하게 회피.
- **클라이언트 압축 실패 시 우회(Bypass) 업로드**: 초고해상도 사진 처리 시 브라우저단 캔버스 압축이나 `FileReader` 변환이 실패(메모리 부족 등)할 경우, 에러 팝업을 띄우지 않고 원본 파일 그대로 백엔드로 넘겨 서버 단에서 WebP로 처리되도록 폴백 로직 고도화 (단, HEIC 포맷은 명시적 차단).
- **업로드 중 글 작성(Submit) 원천 차단**: 이미지가 반투명하게 서버로 업로드 중일 때(Blob 주소 상태) 글 등록을 눌러 타 기기에서 엑스박스가 노출되는 Race Condition을 방지하기 위해, `uploadCount` 상태를 도입하여 [작성] 버튼을 [업로드 중...]으로 잠그고 폼 제출을 차단.

### 5.7 게시판 에디터 미디어 첨부 및 업로드 로직 개편 (`PostMessageForm.js`)
- **유튜브 첨부 포커스 유실 버그 해결**: 모바일 환경에서 유튜브 주소 알림창(`prompt`) 호출 시 에디터 포커스를 잃어 영상이 삽입되지 않던 현상을 `focus()`와 `insertAdjacentHTML` Fallback으로 완전 해결.
- **XSS 필터 우회 oembed 치환 로직**: 백엔드의 엄격한 보안 필터(`BANNED_TAGS` 내 `<iframe`)를 우회하기 위해, 클라이언트 단에서 `onSubmit` 시 에디터 내의 `<iframe>`을 백엔드가 허용하는 `<oembed>` 태그로 변환하여 전송하는 가로채기(Intercept) 로직 구현.
- **영상 하단 줄바꿈 무시(Inline) 현상 해결**: `<iframe>` 치환 시 바깥쪽 반응형 껍데기(`div.ratio`)까지 제거해 버려 영상 옆으로 텍스트가 기어 올라오던 현상을 껍데기 유지 방식으로 수정하여 완벽한 줄바꿈(Block) 구현.
- **프롬프트 입력창 UX 개선**: 링크 및 유튜브 추가 시 불필요한 기본값(value)을 없애고 텍스트 메시지에 예시를 보여주는 방식으로 튜닝하여, 주소 복사-붙여넣기 시 문자가 겹치는 불편함 제거.

### 5.8 업데이트 노트 모달 리뉴얼 (`FeatureHistoryModal.js`)
- **동적 NEW 뱃지 자동화**: 하드코딩되었던 신규 여부(`isNew`)를 버리고, 작성일 기준 **2주(14일)** 이내인지 실시간으로 계산해 파란색(Primary) 뱃지가 자동으로 붙고 꺼지도록(Light) 동적 렌더링 적용.
- **카테고리별 뱃지 컬러링 고도화**: 대주제 카테고리가 닫혀 있을 때도 내부에 최신 내역이 있으면 'NEW' 뱃지가 항상 파란색으로 유지되도록 시인성 강화.
- **UX 라이팅 및 디테일 정제**: 모달 제목을 '업데이트 노트'로 변경, 뱃지 텍스트를 '추가/개선/수정' 등 두 글자 태그로 압축, 기능 설명을 진중하고 전문적인 톤으로 재작성하였으며, 레거시 기능들의 일자를 실제 초기 Git 커밋 히스토리(2022.11)로 동기화.

### 5.9 메인 화면 기본 웹링크 토글 반응성(캐시) 버그 수정 (`Home.js`)
- **문제**: 로그인/로그아웃 시 아폴로(Apollo Client)의 `BUILTIN_WEBLINKS` 캐시가 갱신되어도 화면의 기본 웹링크 토글 상태가 즉각적으로 변하지 않는 버그 발생.
- **원인**: `useQuery`의 `onCompleted` 콜백에서 데이터를 `useState`로 복사해 관리하는 안티 패턴을 사용하여, 캐시 갱신 시 `onCompleted`가 재호출되지 않아 화면(상태)이 서버 캐시와 단절됨.
- **해결**: `useState`를 완전히 제거하고, `useQuery`의 반환값(`data.builtinWeblinks.weblinks`)을 컴포넌트 렌더링 시 직접 참조하도록 변경하여 캐시 업데이트 시 UI가 즉시 반응하도록 수정.

### 5.10 모바일 헤더 유튜브 버튼 줄바꿈/잘림 현상 픽스 (`SearchSelector.js`, `YoutubeOffcanvas.js`)
- **문제**: 모바일 등 가로폭이 좁은 환경에서 검색 영역 우측의 아이콘 컨테이너(`.row`) 공간이 부족할 시, 유튜브 버튼(티커 포함)이 다음 줄로 강제 개행되거나 `overflow-hidden`에 의해 아이콘의 우측이 잘리는 현상 발생.
- **해결**: 컨테이너가 줄바꿈되지 않도록 `flex-nowrap`을 적용하고, 유튜브 재생 티커 영역에 `flex-shrink-1`과 반응형 너비(`min(200px, calc(100vw - 230px))`)를 부여하여 가로 공간 부족 시 티커 텍스트만 자연스럽게 줄어들게 처리. 또한 유튜브 로고 버튼은 `flex-shrink-0`을 명시하여 절대 찌그러지거나 잘리지 않도록 보호.

### 5.11 유튜브 삭제된 영상/재생 불가 영상 자동 건너뛰기 기능 개선 (`YoutubeOffcanvas.js`)
- **상황**: 재생 목록 내에 삭제되거나 국가 제한, 비공개 처리된 영상이 포함되어 있을 경우 재생이 중단되고 플레이어에 에러 메시지가 표시되며 사용자가 직접 다음 곡으로 넘겨야 하는 불편함 존재.
- **개선**: `react-youtube`의 `onError` 이벤트를 감지하여 재생 불가 상태일 경우 1.5초 대기(무한루프 방지) 후 자동으로 다음 곡을 재생하도록 예외 처리 로직 추가.

### 5.12 유튜브 재생 목록 한곡 반복(Single Loop) 기능 추가 (`Controller.js`, `YoutubeOffcanvas.js`)
- **기능**: 유튜브 컨트롤러에 한곡 반복(`play_one.png`) 버튼을 추가하고, 활성화 시 배경색 하이라이트(`bgc-bokk-light`) 적용.
- **동작**: 곡이 종료(`YouTube.PlayerState.ENDED`)되었을 때 `isRepeatOne` 상태가 활성화되어 있으면 다음 곡 대신 현재 곡(`currentVideoIndex`)을 처음부터 다시 자동 재생.

### 5.13 업데이트 내역 모달(Release Note) 최신화 및 푸터 변경 (`FeatureHistoryModal.js`)
- **푸터 텍스트**: 모달 푸터 하단 문구를 'Release Note'로 변경하여 모달의 정체성 명확화.
- **히스토리 데이터 동기화**: 최근 추가/개선된 주요 기능들(웹링크 캐시 반응성 수정, 유튜브 모바일 UI 픽스, 재생 불가 영상 자동 건너뛰기, 유튜브 한곡 반복 기능)을 모달 내역 목록에 모두 최신화 반영.

### 5.14 유튜브 자동 재생(AutoPlay) 상태 동기화 및 실행 버그 수정 (`YoutubeOffcanvas.js`, `Controller.js`)
- **원인 1 (체크박스 비동기)**: `Controller.js`의 옵션 팝오버에서 `<Form.Check>`가 `defaultChecked`를 사용하고 있어 상태 변경 시 체크박스 UI가 갱신되지 않던 문제.
- **원인 2 (Apollo 캐시 단절)**: `useQuery(ME)`의 `onCompleted`에서 로컬 `useState`로 복사하던 구조로 인해 캐시 히트 시 `autoPlay`가 항상 `false`로 남아있던 현상.
- **원인 3 (실행 트리거 부재)**: 플레이어 준비(`onReady`) 및 오프캔버스 열림(`shown.bs.offcanvas`) 시 `autoPlay`가 켜져 있어도 `playVideo()`를 명시적으로 호출하지 않아 브라우저 정책에 의해 자동 재생이 정지되던 현상.
- **해결**: `autoPlay` 상태를 Apollo 캐시와 직접 연동하고 `checked` 제어 컴포넌트로 개편하였으며, `onReady` 및 오프캔버스 오픈 이벤트 리스너를 통해 조건 충족 시 즉각 `playVideo()`가 트리거되도록 수정.

### 5.15 OP.GG 전적 검색 엔드포인트 및 파라미터 최신화 (`GameRecordSearch.js`)
- **기존**: `https://www.op.gg/summoner/` 및 `userName` 파라미터를 사용하던 레거시 검색 방식
- **개선**: OP.GG 최신 검색 라우팅 구조인 `https://op.gg/ko/lol/summoners/search` 및 쿼리 파라미터(`q`, `region=kr`)로 폼 및 빠른 소환사 버튼 링크 전면 갱신.

### 5.16 유튜브 오프캔버스 대화면 확장 및 내장형 도트 핸들 리사이저 (`YoutubeOffcanvas.js`, `YoutubeOffcanvas.module.css`)
- **기존 한계**: 좌측 테두리 밖으로 튀어나온 탭 버튼(`<<`)과 특정 버튼 영역에서만 드래그가 가능한 구조로 인해 화면 가림 및 조작 범위 제한 발생.
- **내장형 도트 핸들(Draggable Handle Dots) 디자인**: 튀어나온 탭 버튼을 제거하고, 오프캔버스 좌측 경계선에 완벽히 일체화된 미니멀 캡슐형 점자 그립(2열 3행 dot style)을 순수 CSS로 구현하여 시각적 완성도와 깔끔한 모던 UI 제공.
- **전체 세로 라인 드래그 인터랙션**: 오프캔버스 좌측 테두리의 높이 100% 전체 라인 어디서나 마우스/터치를 대면 `col-resize` 커서로 전환되며 상단부터 하단까지 자유롭게 드래그 리사이징 지원.
- **호버 및 드래그 시각 피드백**: 마우스 오버 및 드래그 중일 때 세로 전체 라인에 은은한 액센트 블루 하이라이트(`::before`)와 핸들 점 하이라이트가 반응형으로 작동.
- **인터랙션 및 반응형 유지**: 클릭(탭) 시 500px ↔ 최대 폭 토글 기능, 창 너비 1000px 이상 데스크톱 활성화 조건, 드래그 중 iframe 마우스 씹힘 방지 오버레이 모두 완벽 유지.

### 5.17 로컬 개발 환경 Cross-Site 세션 쿠키 차단 방지 (`client.js`)
- **원인**: 브라우저에서 `http://localhost:3000`으로 접속 시 백엔드 요청이 `.env`의 IP(`http://172.30.1.88/api/`)로 전송되어, 서로 다른 도메인(Cross-Site)으로 인식됨에 따라 브라우저 쿠키 보안 정책(SameSite)에 의해 세션 쿠키(`PHPSESSID`)가 차단되어 로그인이 유지되지 않는 현상 발생.
- **해결**: `client.js`에서 접속 호스트(`window.location.hostname`)가 `localhost` 또는 `127.0.0.1`일 경우 동일 호스트인 `http://localhost/api/`로 GraphQL 요청을 보내도록 동적 분기 처리하여, `localhost:3000`과 `172.30.1.88:3000` 양쪽 환경 모두에서 세션 쿠키가 온전히 저장/전송되도록 개선.

### 5.18 다중 태깅 플레이리스트 시스템 구축 (`YoutubeOffcanvas.js`, `PlaylistManageModal.js`, `VideoPlaylistTagModal.js`)
- **다대다(N:M) 태깅 아키텍처**: 한 곡이 여러 개의 플레이리스트에 동시에 속할 수 있는 매핑 구조(`user_youtube_queue_item_playlists`)를 구축하여 유연한 곡 분류 지원.
- **필터 셀렉터 지원**: 오프캔버스 상단에 `[전체 (N)]`, `[등록 안 된 목록 (N)]`, `[플레이리스트별 (N)]`을 선택할 수 있는 드롭다운 셀렉터를 탑재하여 원하는 곡 목록만 즉각 필터링 및 연속 재생.
- **플레이리스트 관리 모달**: 신규 플레이리스트 등록, 실시간 인라인 이름 수정, 플레이리스트 안전 삭제(곡 데이터 보존) 지원.
- **곡 태그 다중 설정 모달**: 각 곡 아이템의 `🏷️ 태그` 버튼을 통해 복수의 플레이리스트를 체크박스로 손쉽게 지정/해제하고 즉시 새 플레이리스트를 추가할 수 있는 편의 UI 제공.
- **연동 재생**: 선택된 플레이리스트에 따라 이전/다음 곡 순회, 한곡 반복, 셔플(랜덤) 재생 및 빈 목록 안내 피드백 완벽 연동.
### 5.19 오프캔버스 - 모달 포커스 트랩(FocusTrap) 충돌 해결 (`YoutubeOffcanvas.js`, `VideoPlaylistTagModal.js`, `PlaylistManageModal.js`)
- **원인 분석**: Bootstrap 5의 네이티브 오프캔버스(`Offcanvas`)는 표시 중일 때 전역 `focusin.bs.focustrap` 이벤트 리스너를 통해 오프캔버스 외부로 나간 포커스를 오프캔버스로 강제 회수함. 그 결과 오프캔버스 외부(Portal)에 렌더링된 플레이리스트 관리/태그 모달 내의 `<input>`을 클릭해도 즉시 포커스가 오프캔버스로 뺏겨 텍스트 입력 및 생성이 불가능했던 버그 발생.
- **포커스 트랩 일시 해제(Deactivate)**: 
  - `YoutubeOffcanvas.js`: 오프캔버스가 열릴 때(`shown.bs.offcanvas`) 오프캔버스 인스턴스의 포커스 트랩을 `bsOffcanvas._focustrap.deactivate()`로 자동 해제하여 오프캔버스가 포커스를 독점하지 않도록 개선.
  - `VideoPlaylistTagModal.js` & `PlaylistManageModal.js`: 모달이 열릴 때 오프캔버스 `_focustrap`을 비활성화하고, 모달이 닫힐 때 원래대로 복원(`activate()`)하는 라이프사이클 훅 구현.
- **인풋 포커스 안전 보장**: 모달 `<Form.Control>`에 `onClick={(e) => e.target.focus()}` 및 `onMouseDown={(e) => e.stopPropagation()}`을 명시하여 이벤트 버블링에 의한 포커스 유실을 원천 차단하고 부드러운 입력 UX 보장.

### 5.20 삭제된 플레이리스트 감지 시 전체(ALL) 목록 자동 복구 및 동기화 (`YoutubeOffcanvas.js`, `Playlist.js`, `PlaylistManageModal.js`)
- **버그 원인**: 사용자가 특정 플레이리스트를 선택한 상태에서 해당 플레이리스트를 삭제하면, `<Form.Select>`의 `<option>` 목록에서는 제거되어 브라우저 UI는 첫 번째 옵션인 `전체`를 보여주지만 React 내부 상태 `selectedPlaylistId`는 삭제된 이전 ID 값을 그대로 유지함. 그 결과 `displayedVideoItems` 필터링에서 삭제된 ID와 매칭되는 곡이 없어 빈 배열이 반환되고, "등록된 영상이 없습니다" 화면이 노출되는 불일치 발생.
- **해결 방안 (3중 안전장치 구축)**:
  1. `displayedVideoItems` 메모이제이션: `selectedPlaylistId`가 현재 `playlists` 목록에 존재하지 않는 경우 즉시 전체 영상(`videoItems`)을 반환하도록 폴백 처리.
  2. `useEffect` 자동 복구: `playlists` 목록 갱신 시 선택된 ID가 유효하지 않으면 `selectedPlaylistId`를 즉시 `"ALL"`로 리셋하고 재생 인덱스를 `0`으로 초기화.
  3. `onDeletePlaylist` 이벤트 연동: `PlaylistManageModal`에서 삭제 확정 시 부모 컴포넌트에 삭제된 ID를 즉각 통지하여 비동기 쿼리 완료 전에도 상태를 동기화.

### 5.21 유튜브 영상 목록 긴 제목 썸네일 하단 개행(줄바꿈) 레이아웃 버그 수정 (`YoutubeVideoItem.js`, `Playlist.js`)
- **버그 원인**: Bootstrap의 `.row`는 기본적으로 `flex-wrap: wrap` 속성을 가지며, 자식 요소인 `.col`은 flexbox 명세상 `min-width: auto` 상태에서 `white-space: nowrap` 텍스트의 전체 길이만큼 최소 너비를 갖게 됨. 제목이 긴 영상(특수문자/일본어/영어 등)의 경우 텍스트 너비가 오프캔버스 가로폭을 초과하여 `.col` 전체(제목, 시간, 태그)가 썸네일 옆이 아닌 썸네일 아래 줄로 강제 개행되는 레이아웃 깨짐 현상 발생.
- **해결 방안**:
  1. `.row`에 `flex-nowrap`을 추가하여 플렉스 아이템이 강제로 다음 줄로 떨어지지 않도록 고정.
  2. 썸네일 컨테이너(`col-auto`)에 `flex-shrink-0`을 부여하여 가로 공간 부족 시에도 썸네일 비율 및 크기(80px) 보호.
  3. 텍스트 컨테이너(`col`)에 명시적인 `style={{ minWidth: 0 }}`을 적용하여 부모 너비에 맞게 유연하게 축소되도록 하고, 긴 제목은 부드럽게 말줄임표(`...`) 처리되도록 개선.

### 5.22 플레이리스트 셀렉터 및 컨트롤러 버튼 수직 기준중심선 정렬 (`Controller.js`, `YoutubeOffcanvas.js`)
- **버그 원인**: `Controller.js`가 레거시 `float-end` 속성을 사용하여 별도의 흐름으로 떠 있었고, 플레이리스트 셀렉터 div가 float 옆으로 비집고 들어가는 구조로 인해 두 컴포넌트 간 수직 정렬(`align-items: center`)이 불가능했음. 또한 셀렉터(높이 31px)와 컨트롤러 버튼(높이 약 38px, 패딩 상이)의 높이 규격이 달라 수직 중심축이 어긋나 삐뚤빼뚤하게 노출됨.
- **해결 방안**:
  1. `Controller.js`의 `float-end`를 제거하고 `d-flex align-items-center` 및 버튼 높이 32px(아이콘 20x20px)로 통일.
  2. `YoutubeOffcanvas.js`에서 플레이어 하단에 `d-flex align-items-center justify-content-between gap-2` 일체형 Flexbox 컨테이너를 구축하여 좌측(셀렉터+관리버튼)과 우측(컨트롤러)의 수직 중심선이 1px의 오차도 없이 완벽히 일치하도록 개편.

### 5.23 플레이리스트 탐색(View)과 재생 큐(Queue) 분리 아키텍처 구축 (`YoutubeOffcanvas.js`, `YoutubeVideoItem.js`)
- **기존 문제**: 플레이리스트 셀렉터를 변경하면 화면 표시 목록(`displayedVideoItems`)이 바뀌면서 상단 플레이어(`YoutubePlayer`)의 `firstVideoId`와 `currentVideoIndex`가 즉시 0으로 리셋되어, 사용자가 듣고 있던 영상이 강제로 끊기고 새 플레이리스트의 첫 곡으로 바뀌는 불편함 존재.
- **해결 방안 (View와 Playback Queue 분리)**:
  1. **탐색 목록(View)과 재생 큐(Active Queue) 분리**: `selectedPlaylistId` 변경 시에는 화면에 보여지는 탐색 목록(`displayedVideoItems`)만 바뀌고, 현재 재생 큐(`activeQueue`), 재생 중인 비디오 ID(`currentVideoId`), 재생 인덱스는 전혀 변경되지 않고 기존 재생 상태를 완벽하게 유지.
  2. **명시적 클릭 시 큐 전환 (`handleSelectVideo`)**: 사용자가 탐색 중인 플레이리스트 목록에서 특정 곡을 직접 클릭했을 때 비로소 `activeQueue`가 해당 플레이리스트로 확정되고 해당 곡부터 재생을 시작하도록 개선.
  3. **재생 큐 기준 순회**: 다음 곡/이전 곡/셔플/한곡 반복/에러 자동 건너뛰기 역시 현재 활성 재생 큐(`currentQueue`)를 기준으로 동작하도록 정밀 연동.
  4. **현재 재생 중 실시간 하이라이트**: 인덱스 기반 비교 대신 실제 재생 중인 유튜브 비디오 ID(`isCurrentPlaying={item.video.id === effectivePlayingVideoId}`)를 비교하여, 다른 플레이리스트를 탐색 중일 때도 현재 재생 곡에만 정확히 하이라이트가 표시되도록 시각적 피드백 개선.
  5. **플레이어 DOM 유지**: 빈 플레이리스트를 탐색할 때도 플레이어와 컨트롤러가 DOM에서 사라지지 않고 계속 재생되도록 렌더링 조건 완화.

### 5.24 현재 재생 중인 플레이리스트 시각적 표시 및 바로가기 연동 (`YoutubeOffcanvas.js`)
- **도입 배경**: 사용자가 재생 중인 곡을 유지하면서 다른 플레이리스트를 탐색(브라우징)할 수 있게 됨에 따라, 현재 어떤 플레이리스트가 재생되고 있는지 한눈에 인지할 수 있는 표시 및 조작 편의 기능 필요.
- **구현 세부사항**:
  1. **오프캔버스 헤더 뱃지**: 오프캔버스 상단 제목 우측에 현재 재생 중인 플레이리스트 이름과 아이콘(`🎵 전체`, `📂 등록 안 된 목록`, `🏷️ 플레이리스트명`)을 펄스 스피너와 함께 뱃지로 상시 표시.
  2. **셀렉터 드롭다운 옵션 하이라이트**: 드롭다운 옵션 텍스트를 복잡하게 만들지 않고, 현재 재생 중인 플레이리스트 `<option>`에 은은한 배경색(`backgroundColor: #e7f5ff`)과 굵은 폰트(`fontWeight: 600`, `color: #1864ab`) 스타일을 적용하여 시각적으로 자연스럽게 강조.
  3. **탐색 중 재생 목록 안내 & 원클릭 복귀 칩**: 사용자가 현재 재생 중인 플레이리스트와 다른 플레이리스트를 탐색할 때, 셀렉터 하단에 `▶ 재생 중: [🏷️ 플레이리스트명]` 및 `[재생 목록 보기 ➔]` 버튼을 노출하여 클릭 한 번으로 재생 중인 목록으로 즉시 돌아올 수 있도록 UX 개선.
  4. **헤더 티커 툴팁 연동**: 상단 헤더의 실시간 재생 티커 툴팁(`title`)에도 현재 재생 중인 플레이리스트명을 함께 표기(`재생 중: [플레이리스트명] 곡제목`).
  5. **동적 삭제 동기화**: 재생 중인 플레이리스트가 관리 모달에서 삭제될 경우 자동으로 `ALL(전체)`로 안전하게 폴백 복구.

### 5.25 플레이리스트 셀렉터 커스텀 드롭다운(Dropdown) 전환, 이모지 제거 및 '미분류' 개편 (`YoutubeOffcanvas.js`, `YoutubeOffcanvas.module.css`, `Playlist.js`, `Controller.js`)
- **원인 분석**: 브라우저(특히 윈도우 Chromium/크롬/엣지) 표준 명세상 네이티브 `<select>`의 드롭다운 팝업(`<option>`)은 OS 네이티브 위젯 레이어에서 렌더링되므로 CSS `padding`, `margin`, `height` 속성이 강제로 무시되어 행 간격 확장이 불가능한 한계 존재.
- **해결 방안 (React-Bootstrap `Dropdown` 전면 개편 & UX 정제)**:
  1. **완전한 DOM 기반 드롭다운 전환**: 네이티브 `<select>` 대신 React-Bootstrap의 `<Dropdown>`, `<Dropdown.Toggle>`, `<Dropdown.Menu>`, `<Dropdown.Item>`으로 전환하여 100% 스타일링 가능한 커스텀 드롭다운 구축(유튜브 오프캔버스 및 재생 목록 관리 페이지 `Playlist.js` 전면 통일).
  2. **넉넉한 항목별 패딩 확보**: 드롭다운 메뉴 항목(`.dropdownItem`)에 `padding: 8px 14px`을 부여하여 각 플레이리스트 행의 상하 높이(약 38px)와 터치/클릭 영역을 시원하게 확보.
  3. **이모지 제거 및 직관적 용어 개편**: 드롭다운 토글 및 목록에서 불필요한 이모지(`🎵`, `📂`, `🏷️`)를 전면 제거하여 모던하고 정돈된 타이포그래피를 적용하였으며, 다소 길고 번잡했던 "등록 안 된 목록" 문구를 3글자의 표준 UI 용어인 **"미분류"**로 전면 교체(오프캔버스 및 플레이리스트 관리 페이지 통일).
  4. **재생 중 & 선택 항목 및 메인 버튼 뱃지 일체화**:
     - 드롭다운 메인 토글 버튼: 단순 텍스트 `전체 (5)` 대신 좌측 이름과 우측 곡 수 뱃지(`badge rounded-pill bg-light text-secondary border`)를 드롭다운 목록과 동일하게 통일 적용.
     - 현재 재생 중인 플레이리스트: 부드러운 스카이블루 배경(`.playingItem: #e7f5ff`, 호버 시 `#d0ebff`) 및 블루 텍스트 하이라이트.
     - 현재 선택(탐색) 중인 플레이리스트: 볼드 폰트 및 앞에 파란색 체크마크(`✓`) 표시.
     - 우측 뱃지: 각 플레이리스트에 등록된 곡 수(`(N)`) 뱃지 정렬.
  5. **주변 컨트롤 버튼 높이 및 여백 일치**: 토글 버튼(높이 38px), "⚙️ 관리" 버튼(높이 38px), 우측 컨트롤러 버튼(`36x36px`)의 비율 및 상하 마진(`mb-3`)을 함께 조정하여 세로 축 기준 중심선과 비례감을 완벽히 유지.

### 5.26 유튜브 재생목록 모바일 반응형 사용자 경험(UX) 고도화 (`YoutubeOffcanvas.js`, `YoutubeOffcanvas.module.css`, `Controller.js`, `Controller.module.css`, `YoutubeVideoItem.js`)
- **도입 배경**:
  모바일 뷰포트(360px~480px) 환경에서 드롭다운, 관리 버튼, 컨트롤러 버튼 6개가 가로 1열에 억지로 끼워져 드롭다운이 극단적으로 찌그러지거나 잘리는 현상, 작은 화면 높이로 인한 이중 스크롤(오프캔버스 바디 스크롤 + 곡 목록 스크롤 충돌), 터치 조작 시 오터치(곡 재생 vs 태그 버튼) 문제 발생.
- **해결 방안 및 모바일 UX 최적화**:
  1. **스마트 2단 반응형 컨트롤 바 (`@media (max-width: 480px)`)**:
     - 데스크톱/태블릿(가로폭 481px 이상): 기존과 동일하게 드롭다운 + 관리 버튼(좌측)과 6개 컨트롤러 버튼(우측)이 1열 양 끝 정렬(`justify-content-between`).
     - 소형 모바일(가로폭 480px 이하): 1행에 드롭다운(폭 100% 확장)과 ⚙️ 관리 버튼 배치, 2행에 6개 컨트롤러 버튼을 가로 균등 분할(`justify-content: space-between`) 독(Dock) 형태로 2단 자동 분리 배치하여 어떤 소형 기기에서도 드롭다운과 버튼이 찌그러지지 않도록 원천 개선.
  2. **모바일 단일 뷰포트 스크롤 (`flex-grow: 1; min-height: 0;`) & 이중 스크롤 제거**:
     - 기존의 고정 높이(`height: 400px`)를 완전히 제거하고, `offcanvas-body`를 `d-flex flex-column h-100 overflow-hidden` 구조로 개편.
     - 상단 플레이어와 컨트롤 바는 고정 뷰로 안정감 있게 노출되고, 하단 재생 목록 리스트(`#scroll_queue`)가 가용 화면 높이를 100% 동적으로 채우며 단독 스크롤되도록 구축하여 모바일 주소창 여닫힘이나 작은 세로 높이(아이폰 SE 등)에서도 플레이어가 화면 밖으로 밀려나지 않도록 개선.
  3. **모바일 오프캔버스 전폭(100vw) 자동 확장**:
     - 모바일 화면(`windowWidth < 500`)에서는 오프캔버스 너비를 `100vw`로 자동 확장하여 답답함 없이 시원한 풀스크린 플레이어 뷰 제공 (데스크톱에서는 기존 500px 및 리사이징 유지).
  4. **모바일 터치 타겟 분리 및 햅틱 피드백 (`YoutubeVideoItem.js`, `Controller.module.css`)**:
     - 곡 아이템 내의 `+ 태그` 텍스트 링크를 명확한 모바일 소프트 칩 버튼(`.btn-light.border.py-0.px-2`)으로 분리하여, 엄지손가락 터치 시 곡 재생(`onClick`)과의 겹침/오동작을 완벽히 방지.
     - 컨트롤러 버튼에 `:active { transform: scale(0.9); }` 탭 반응 애니메이션을 추가하여 모바일 터치 시 명확한 물리적 피드백 제공.
  5. **모바일 태그 모달 인풋 자동 포커스 제거 (`VideoPlaylistTagModal.js`)**:
     - 곡 태그 설정 모달 열림 시 `Modal.onEntered`에서 새 플레이리스트 인풋에 강제 포커스를 주던 타이머 로직을 제거하여, 모달 오픈 시 가상 키보드가 불필요하게 팝업되어 화면과 체크박스를 가리던 모바일 사용성 문제를 해결 (사용자가 직접 인풋을 탭할 때만 키보드 활성화).

### 5.27 플레이리스트 곡 개수 산정 불일치 버그 수정 (`VideoPlaylistTagModal.js`, `PlaylistManageModal.js`, `YoutubeOffcanvas.js`, `Playlist.js`, `lib/bokk/db.php`, `public/api/index.php`)
- **문제 현상**: 유튜브 오프캔버스 드롭다운에서는 특정 플레이리스트의 곡 수가 2곡으로 표시되는데, 곡 태그 설정 모달(`VideoPlaylistTagModal`)에서는 3곡으로 불일치하게 표시되는 현상 발생.
- **원인 분석**:
  1. **백엔드 고아(Orphan) 레코드 및 카운트 쿼리 오류**: 곡 삭제(`deleteVideoItem`) 시 `user_youtube_queue_items`에서만 삭제되고 매핑 테이블(`user_youtube_queue_item_playlists`)에서는 연쇄 삭제되지 않아 고아 레코드가 잔존함. 또한 `get_user_playlists` 쿼리가 `COUNT(DISTINCT m.queue_item_number)`를 수행하여 이미 삭제된 곡의 매핑까지 카운트되어 3곡으로 집계됨.
  2. **클라이언트 모달 정적 참조**: 모달 컴포넌트들이 클라이언트 최신 `videoItems` 상태 대신 서버 `MY_PLAYLISTS` 응답의 `pl.videoCount`만을 정적으로 참조하여 차이가 발생.
- **해결 방안**:
  1. **DB 쿼리 개선 (`db.php`)**: `COUNT(DISTINCT m.queue_item_number)` ➔ `COUNT(DISTINCT q.number)`로 변경하여 실제 존재하는 영상만 정확히 집계하도록 수정.
  2. **곡 삭제 연쇄 처리 (`index.php`)**: `deleteVideoItem` 리졸버에서 곡 삭제 시 매핑 테이블(`user_youtube_queue_item_playlists`)에서도 즉시 연쇄 삭제하도록 보완.
  3. **기존 고아 레코드 정리**: DB 상에 이미 삭제된 곡 번호를 가리키던 잔여 매핑 레코드 1건 삭제 정리.
  4. **프론트엔드 실시간 동기화**: `VideoPlaylistTagModal` 및 `PlaylistManageModal`에 `videoItems` props를 전달하고 클라이언트 실시간 필터링(`videoItems.filter(...)`)을 우선 적용하여, 항상 드롭다운과 100% 동일하고 정확한 곡 수를 표시하도록 이중 안전장치 구축.

### 5.28 플레이리스트 담기 UX 및 버튼 직관성 개선 (`YoutubeVideoItem.js`, `VideoPlaylistTagModal.js`)
- **도입 배경**: 첫 사용자 입장에서 `🏷️ 태그 1`, `🏷️ + 태그` 등의 모호한 표현이 "1번 태그인가?", "SNS 해시태그인가?" 혼란을 유발하고, 실제 플레이리스트에 곡을 담는 기능이라는 인지를 저해함.
- **개선 내용**:
  1. **음악 앱 표준 용어 '+ 담기' / '담김 N' 적용 (`YoutubeVideoItem.js`)**:
     - 미등록 곡: `+ 담기` (회색 소프트 테두리 버튼)
     - 등록된 곡: `담김 N` (은은한 블루 하이라이트 배경 `#e7f5ff`, 텍스트 `text-primary`, 볼드 적용)
     - 툴팁: `플레이리스트에 담기 / 변경`으로 액션의 정체성 명확화.
  2. **모달 타이틀 및 라이팅 정제 (`VideoPlaylistTagModal.js`)**:
     - 타이틀: `🏷️ 플레이리스트 태그 설정` ➔ `📑 플레이리스트에 담기`
     - 안내문: `이 곡을 담을 플레이리스트를 선택하세요 (복수 선택 가능)`
     - 저장 버튼: `태그 저장` ➔ `담기 완료`

### 5.29 곡 이동 버튼 클릭 시 현재 재생 중인 플레이리스트 화면 전환 후 곡 이동 연동 (`YoutubeOffcanvas.js`)
- **도입 배경**: 사용자가 특정 플레이리스트를 재생하면서 다른 플레이리스트나 전체/미분류 목록을 둘러보던 중 컨트롤러의 이전 곡(`|<<`) 또는 다음 곡(`>>|`) 버튼을 눌렀을 때, 화면은 다른 목록에 그대로 머물러 있어 어떤 곡으로 넘어갔는지 시각적으로 확인하기 어려운 단절감 발생.
- **개선 내용**:
  1. **재생 플레이리스트 화면 자동 복귀 연동**: 이전 곡(`handlePreviousPlay`) 및 다음 곡(`handleNextPlay`) 버튼을 누를 때 현재 보고 있는 플레이리스트(`selectedPlaylistId`)가 현재 재생 중인 플레이리스트(`playingPlaylistId`)와 다르면, `setSelectedPlaylistId(playingPlaylistId)`를 호출하여 **재생 중인 플레이리스트 화면으로 즉시 전환한 후 곡 이동을 수행**.
  2. **새 목록 내 자동 스크롤 연동**: 화면 전환 후 렌더링 타이밍을 고려해 `setTimeout(..., 50)` 기반으로 새 플레이리스트 목록 내에서 현재 재생되는 곡의 위치로 큐 스크롤이 부드럽게 자동 이동하여 하이라이트 노출.

### 5.30 플레이리스트 8자리 공유코드 발급 및 복사 생성(가져오기) 시스템 구축 (`PlaylistManageModal.js`, `querys.js`, `lib/bokk/db.php`, `public/api/schema.graphql`, `public/api/index.php`)
- **도입 배경**: 사용자가 자신이 만든 유튜브 플레이리스트를 다른 기기나 지인에게 공유하고, 공유받은 코드를 입력하여 자신의 계정으로 즉시 플레이리스트 및 곡들을 복사 생성(가져오기)할 수 있는 편의 기능 구축.
- **구현 내용**:
  1. **오타 방지 8자리 고유 공유코드 체계 (`db.php`)**:
     - 시각적으로 혼동되기 쉬운 문자(`0`, `O`, `1`, `I`)를 제외한 32개 영숫자 난수 조합으로 8자리 고유 코드 자동 발급 (`generate_unique_share_code`).
     - `user_youtube_playlist.share_code` 컬럼 자동 마이그레이션 및 기존 데이터 일괄 발급 처리.
  2. **스마트 곡 복사 및 큐 중복 방지 (`import_playlist`)**:
     - 원본 플레이리스트의 수록곡들을 복사할 때, 사용자의 기존 큐에 이미 존재하는 비디오는 큐 중복 생성 없이 새 플레이리스트 매핑만 연결.
     - 내 큐에 없는 곡들만 유저 큐(`user_youtube_queue_items`)에 자동 추가한 후 새 플레이리스트에 매핑하여 재생 목록의 청결성 보장.
  3. **GraphQL API 확장 (`schema.graphql`, `type.php`, `index.php`)**:
     - `Playlist.shareCode: String!` 필드 추가
     - `importPlaylist(shareCode: String!, name: String): Playlist!` Mutation 구축 및 캐시 스키마 갱신.
  4. **플레이리스트 관리 모달 UI 연동 (`PlaylistManageModal.js`)**:
     - 상단에 `[공유코드로 플레이리스트 가져오기]` 인풋 및 `[📥 가져오기]` 버튼 탑재.
     - 각 플레이리스트 행에 고유 공유코드 텍스트 노출 및 `[🔗 공유]` 버튼 클릭 시 클립보드 자동 복사 및 `[✓ 복사됨]` 2초 시각 피드백 연동.
     - 복사 완료 시 `MY_PLAYLISTS` 및 `MY_VIDEO_ITEMS` 자동 갱신.

### 5.31 플레이리스트 담기 모달 체크박스 라벨 이중 버블링 버그 수정 (`VideoPlaylistTagModal.js`)
- **원인 분석**: `Form.Check`의 `<label for="...">`를 클릭했을 때 브라우저 네이티브 명세상 label 클릭 이벤트와 내부 checkbox input 클릭 이벤트가 연속으로 발생하여 부모 `ListGroup.Item`의 `onClick` 핸들러(`handleToggle`)로 2회 중복 버블링됨. 그 결과 React 상태가 '선택 ➔ 선택 해제'로 즉시 원복되어 포커스 링만 남고 체크가 되지 않는 현상 발생.
- **해결 방안**:
  - `ListGroup.Item`을 `role="checkbox"` 및 `as="div"` 기반의 단일 클릭 타겟으로 통합하고, 내부의 체크박스 및 텍스트 요소에 `pointerEvents: "none"`을 부여하여 브라우저의 이중 이벤트 디스패치를 원천 차단.
  - 라벨 텍스트, 체크박스 아이콘, 행 여백 어디를 클릭하든 단 1회 정확하게 토글되도록 개편하고, 키보드 접근성(Space/Enter) 및 더블탭 텍스트 선택 방지(`user-select-none`) 적용.

### 5.32 플레이리스트 담기 모달 PC 사용자 경험(UX) 고도화 (`VideoPlaylistTagModal.js`, `VideoPlaylistTagModal.module.css`)
- **도입 배경**:
  모바일 환경 최적화(키보드 팝업 방지를 위한 자동 포커스 제거 등) 이후, 데스크톱(PC) 환경에서는 키보드 단축키 생산성, 마우스 호버 및 포커스 시각 피드백, 세련된 커스텀 스크롤바 등 PC 환경에 특화된 사용자 경험이 필요함.
- **개선 내용**:
  1. **PC 단축키 생산성 (`Ctrl + Enter` / `Cmd + Enter`)**:
     - 모달 어디서나 `Ctrl + Enter` (Mac은 `Cmd + Enter`) 키를 누르면 마우스로 아래 버튼을 누르지 않아도 즉시 저장을 수행하여 키보드 중심의 빠른 작업 흐름 보장.
     - 하단 [담기 완료] 버튼에 PC 화면에서만 깔끔하게 노출되는 단축키 힌트 배지(`Ctrl+↵`, `styles.shortcutBadge`) 제공.
  2. **PC 스마트 포커스 유지 및 자동 포커스**:
     - 등록된 플레이리스트가 0개일 때는 모달 오픈 즉시 새 플레이리스트 입력 인풋(`inputRef`)에 스마트 자동 포커스.
     - PC 환경에서 새 플레이리스트를 생성한 후에도 인풋 포커스가 풀리지 않고 유지되어 연속적인 플레이리스트 생성이 가능하도록 개선.
  3. **체크 및 호버 시각 피드백 (`styles.playlistRow`, `styles.checkedRow`)**:
     - 체크된 플레이리스트 행 전체에 부드러운 스카이블루 배경색(`#f0f7ff`, 호버 시 `#e7f5ff`)과 텍스트/뱃지 볼드 하이라이트를 적용하여 넓은 데스크톱 모니터에서도 선택 여부를 직관적으로 인지.
     - 키보드 탭 탐색(`Tab`) 시 명확한 블루 포커스 링(`:focus-visible`) 피드백 제공.
  4. **데스크톱 슬림 커스텀 스크롤바 (`styles.listContainer`)**:
     - 플레이리스트가 많아 스크롤이 발생할 때 PC 브라우저 특유의 둔탁하고 두꺼운 기본 스크롤바 대신, 5px 두께의 미니멀 모던 슬림 스크롤바를 적용하여 세련된 데스크톱 UI 완성.

### 5.33 유튜브 오프캔버스 헤더 라이브 뱃지 및 영상 목록 썸네일 UI/UX 고도화 (`YoutubeOffcanvas.js`, `YoutubeOffcanvas.module.css`, `YoutubeVideoItem.js`, `Playlist.js`)
- **문제점**:
  - 기존의 `●재생 중: 전체` 뱃지는 점(`spinner-grow`)과 텍스트 사이 간격 부재(`gap-1.5` 유효하지 않음), 타원형 테두리(pill)의 협소한 좌우 패딩으로 인해 글자가 테두리에 갇혀 극도로 답답해 보이는 시각적 문제 발생.
  - 헤더 뱃지 폰트 크기가 다소 작아 가독성이 떨어졌으며, 영상 아이템의 썸네일 너비(80px)가 작고 저화질 4:3 썸네일로 인해 상하 검은 여백(레터박스)이 생기는 현상 존재.
- **개선 내용**:
  1. **오디오 이퀄라이저 애니메이션 탑재 (`.equalizer`, `.equalizerBar`)**:
     - 칙칙한 점 대신 음악 앱 특유의 세련된 3단 미니 이퀄라이저 바를 도입.
     - 재생 중일 때 리드미컬하게 오르내리는 부드러운 웨이브 애니메이션(`bounce`), 일시정지 시 차분하게 정지된 그레이 바로 상태를 직관적으로 전달.
  2. **시각적 계층 분리, 폰트 가독성 및 시원한 여백 확보**:
     - 폰트 크기를 `11px`/`11.5px`에서 `12px`/`12.5px`로 자연스럽게 상향하고, 이퀄라이저 높이(13px) 및 패딩(`padding: 4px 12px 4px 10px`)을 넉넉하게 확장하여 답답함을 해소.
     - 보조 레이블인 `재생 중` (슬레이트 그레이)과 핵심 명칭인 `플레이리스트명` (또렷한 다크 볼드) 사이에 1px 버티컬 디바이더(`.badgeDivider`)를 배치하여 정갈한 오디오 UI 감성 구현.
     - 탐색 복귀 바(`재생 목록 보기 ➔`) 폰트와 패딩도 함께 정돈(`12px`).
  3. **썸네일 축소 자글자글(Aliasing) 현상 해결 및 고화질(HD) 스마트 폴백**:
     - `RESOLUTION.MAX` (`maxresdefault.jpg`, 1280x720)를 우선 로드하고, 미지원 영상은 `RESOLUTION.MQ` (`mqdefault.jpg`, 320x180)로 자동 폴백되는 `onError` 안전장치 구축.
     - `transform: translateZ(0)` 및 `backface-visibility: hidden`을 적용하여 브라우저 GPU 밉맵 안티앨리어싱을 강제 활성화함으로써 다운스케일링 시 텍스트나 선화가 자글자글하게 깨지는 픽셀 계단현상을 완전히 제거.
     - 썸네일 래퍼(`.thumbnailWrapper`, 104x58px)에 섬세한 1px 보더와 마이크로 섀도우를 부여하여 경계면 완성도 극대화.
  4. **재생 목록 아이템 UI 전면 개편 (`YoutubeVideoItem.module.css`)**:
     - **재생 중인 곡 하이라이트**: 채도가 강했던 기존 파란 배경 대신, 눈이 편안한 소프트 스카이 틴트(`#f0f7ff`)와 좌측 3.5px 액센트 블루 인디케이터 바(`.activeItem`)를 적용하여 모던 오디오 앱 감성 구현.
     - **복수 태그 줄바꿈 방지 및 콤팩트화**: 등록된 플레이리스트 태그들이 4~6개씩 2~3줄로 지저분하게 개행되어 행 높이가 제각각 널뛰던 문제를, **최대 2개 노출 + `+N` 요약 배지(`.tagMoreBadge`)**와 단일 행(`flex-nowrap`, `.tagContainer`)으로 개편하여 모든 영상 아이템의 세로 높이를 균일하고 단정하게 통일.
  5. **초소형 모바일(380px 이하) 반응형 배려**:
     - 화면 폭이 380px 이하인 초소형 화면에서는 `재생 중 |`을 자동으로 숨기고 `[ ılı 플레이리스트명 ]` 형태로 축약하여 헤더 줄바꿈 및 잘림을 완벽 방지.
  6. **대화면(최대 너비 1000px 확장) UI 밸런스 및 사용자 경험 최적화**:
     - **대화면 풀스크린 플레이어 (`.playerWrapper`)**: 대화면 확장의 핵심 사용 목적인 "시원하고 웅장한 대형 영상 감상"에 맞추어 오프캔버스 가로폭 100%를 가득 채우는 풀스크린 뷰를 제공.
     - **드롭다운 토글 너비 안정화 (`.selectorGroup`)**: 대화면에서 `flex-grow: 1`로 인해 드롭다운 토글이 휑하게 600px 이상 늘어나던 현상을 `flex: 0 1 320px`로 정돈하여 컨트롤러 버튼들과의 시각적 균형감 완성.
     - **재생 목록 슬림 커스텀 스크롤바 (`.queueContainer`)**: 윈도우 OS 특유의 투박하고 두꺼운 16px 기본 사각 스크롤바 대신 6px 너비의 세련된 라운드 슬림 스크롤바를 적용.

### 5.34 헤더 실시간 유튜브 재생 티커 UI 모던 리뉴얼 (`YoutubeOffcanvas.js`, `YoutubeOffcanvas.module.css`)
- **문제점**:
  - 검색창 우측 유튜브 버튼 옆의 실시간 재생 티커 뱃지가 과거의 원색 분홍색 톤(`bg: #fff0f3`, `border: #ffccd5`, `text: #d90429`)과 7px 빨간 점(`spinner-grow text-danger`), 이모지(`🎵`)로 남아있어, 리뉴얼된 전체 오디오 테마(소프트 슬레이트 / 라이브 이퀄라이저)와 이질감 유발.
- **개선 내용**:
  1. **라이브 오디오 이퀄라이저 애니메이션 (`ılı`) 통합**:
     - 칙칙한 단색 빨간 점 대신 오프캔버스 헤더와 동일한 3단 미니 오디오 이퀄라이저 바를 적용하여 음악 재생 시 생동감 있는 파란색 웨이브 애니메이션 연출.
  2. **모던 소프트 테마 동기화 (`.headerTickerBadge`)**:
     - 배경을 소프트 슬레이트 화이트(`#f8fafc`), 테두리를 `#e2e8f0`, 폰트 컬러를 또렷한 다크 슬레이트(`#1e293b`)로 일원화하고 호버 인터랙션(`background: #f1f5f9`, 테두리 `#cbd5e1`, 섀도우) 부여.
  3. **가장자리 페이드 마스크 & 미들 도트 롤링 (`.tickerMarqueeWrapper`, `.tickerMarquee`)**:
     - `mask-image: linear-gradient(...)`를 적용하여 텍스트가 좌우 테두리 안쪽으로 부드럽게 페이드인/아웃되도록 프리미엄 시각 효과 구현.
     - 이모지 대신 정갈한 미들 도트(`·`)로 곡 제목을 무한 롤링.
     - 자동 재생 알림 뱃지(`.tickerNoticeBadge`)도 소프트 블루 톤으로 일체화.

### 5.35 탐색 목록 복귀 안내 바(Return Banner) UI 전면 개편 (`YoutubeOffcanvas.js`, `YoutubeOffcanvas.module.css`)
- **문제점**:
  - 다른 플레이리스트를 둘러볼 때 뜨는 `▶ 재생 중: [이름]  재생 목록 보기 ➔` 바가 세로로 납작하게 찌그러져 있고(`py-1.5`), 폰트가 작으며(`11.5px`), 밋밋한 회색(`bg-light`)으로 답답함을 유발.
- **개선 내용**:
  1. **시원한 여백 및 가독성 확보 (`.returnBanner`)**:
     - 상하/좌우 패딩을 `padding: 8px 12px`로 시원하게 확장하고, 폰트 크기를 `12.5px` / `13px`(볼드)로 키워 가독성을 대폭 향상.
  2. **오디오 이퀄라이저 애니메이션 (`ılı`) 일체화 (`.returnBannerLeft`)**:
     - 둔탁한 파란 삼각형(`▶`) 대신 3단 미니 오디오 이퀄라이저 바를 적용하여 재생 중인 곡의 라이브감을 헤더/티커와 일관성 있게 전달.
     - `재생 중` 보조 레이블과 플레이리스트 이름 사이에 1px 버티컬 디바이더(`.returnDivider`)를 배치하여 정갈하게 정돈.
  3. **인터랙티브 화이트 카드 버튼 (`.returnBtn`)**:
     - 밋밋한 텍스트 링크 대신 입체감 있는 화이트 미니 버튼(`[ 재생 목록 보기 ➔ ]`)으로 전환하여, 호버 시 브랜드 블루 배경과 그림자 피드백을 제공하고 클릭 타겟을 명확화.

### 5.36 기본/최소 너비(500px) 컨트롤 바 줄바꿈 방지 및 플렉스 비율 최적화 (`YoutubeOffcanvas.module.css`)
- **원인 분석**:
  - 데스크톱 오프캔버스 기본 너비(500px, 내부 패딩 제외 가용 폭 468px)에서 `.controlBar`에 `flex-wrap: wrap`이 지정되어 있고 `.selectorGroup`이 `flex: 0 1 320px`로 공간을 차지함에 따라, 6개 컨트롤러 버튼(약 220px)이 공간 부족으로 2번째 줄로 어색하게 떨어지는 레이아웃 깨짐 현상 발생.
- **해결 방안**:
  1. **1열 고정 (`flex-wrap: nowrap`)**: `.controlBar` 및 `.returnBanner`에 `flex-wrap: nowrap`을 명시하여 데스크톱 환경에서 절대로 2줄로 줄바꿈되지 않도록 고정.
  2. **유연한 플렉스 비율 (`flex: 1 1 0; max-width: 360px;`)**: 기본 500px 최소 너비에서는 드롭다운이 230px 안쪽으로 유연하게 축소되어 컨트롤러 버튼 6개와 완벽한 1줄을 유지하고, 1000px 대화면에서는 360px 상한으로 휑하게 찢어지지 않도록 반응형 밸런스 완성.

### 5.37 플레이리스트 담기 모달 하단 버튼 디자인 정제 (`VideoPlaylistTagModal.js`, `VideoPlaylistTagModal.module.css`)
- **문제점**:
  - `[담기 완료]` 버튼 내부에 사각 박스로 끼워 넣은 단축키 뱃지(`Ctrl+↵`)로 인해 시각적으로 조잡해 보이고 버튼 밸런스가 무너짐.
  - 취소 버튼이 칙칙하고 어두운 회색 솔리드(`btn-secondary`)로 되어 있어 메인 액션 버튼과의 대비가 투박함.
- **개선 내용**:
  1. **단축키 뱃지 제거 및 심플화**: 버튼 내부의 사각 뱃지를 완전히 제거하여 깔끔하고 세련된 `[담기 완료]` 버튼([`.saveBtn`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/VideoPlaylistTagModal.module.css#L63-L78))으로 정돈 (단축키 기능 및 툴팁 안내는 완벽 유지).
  2. **모던 라이트 취소 버튼 ([`.cancelBtn`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/VideoPlaylistTagModal.module.css#L47-L61))**: 칙칙한 `btn-secondary` 대신 은은한 라이트 그레이 배경과 섬세한 보더가 적용된 버튼으로 교체하여 부드러운 시각적 계층 분리.
  3. **`+ 추가` 버튼 정돈 ([`.createBtn`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/VideoPlaylistTagModal.module.css#L80-L84))**: 입력창과의 일체감을 높여 깔끔하게 정돈.

### 5.38 플레이리스트 담기 모달 사이즈 및 목록 여백 최적화 (`VideoPlaylistTagModal.js`, `VideoPlaylistTagModal.module.css`)
- **문제점**:
  - 기존 `size="sm"` 속성으로 인해 모달 가로폭이 300px로 극도로 좁게 고정되어, 영상 제목이 급격히 잘리고 플레이리스트 이름과 체크박스, 인풋이 답답하게 끼어 있는 현상 발생.
  - 목록 높이(`max-height: 240px`)로 인해 6번째 플레이리스트가 아래 테두리에서 어색하게 반쯤 잘려 수직적으로도 답답함을 유발.
- **개선 내용**:
  1. **모달 가로폭 최적 확장 (`max-width: 440px;`)**: `size="sm"`을 제거하고 [`.modalDialog`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/VideoPlaylistTagModal.module.css#L1-L6) 클래스를 적용하여 가로폭을 300px에서 440px로 약 47% 확장. 긴 영상 제목 및 플레이리스트 이름이 여유롭게 노출되고, 모바일에서는 `calc(100% - 32px)`로 부드럽게 반응형 적응.
  2. **목록 가용 높이 확장 (`max-height: 300px;`)**: `.listContainer`의 최대 높이를 240px에서 300px로 늘려 6~7개의 플레이리스트가 잘림 없이 온전히 표시되도록 시각적 개방감 확보.
  3. **안내 문구 가독성 튜닝**: 모달 상단 가이드 텍스트의 폰트 크기를 `11px`에서 `12px`로 소폭 상향하여 눈이 편안한 밸런스 완성.

### 5.39 담기 완료 버튼 고대비 완화 및 소프트 틴트 블루 스타일링 (`VideoPlaylistTagModal.module.css`)
- **문제점**:
  - `담기 완료` 버튼이 짙고 채도 높은 솔리드 블루(`#0d6efd`)와 흰색 텍스트로 구성되어, 밝고 은은한 파스텔 톤의 모달 및 라이트 그레이 취소 버튼 사이에서 지나치게 강한 시각적 대비와 부담감을 유발함.
- **개선 내용**:
  1. **소프트 틴트 블루 스타일 ([`.saveBtn`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/VideoPlaylistTagModal.module.css#L71-L93))**:
     - 배경을 부드러운 스카이블루(`#e7f5ff`), 테두리를 세련된 스카이 블루 라인(`#74c0fc`), 텍스트를 또렷하고 깊이감 있는 브랜드 블루(`#1971c2`)로 개편하여 전체 모달 및 취소 버튼과의 시각적 대비를 부드럽게 완화.
     - 기본 상태에서는 눈이 편안한 파스텔 톤을 유지하면서도 테두리와 볼드 텍스트로 저장(Primary) 액션의 성격을 명확히 전달.
  2. **인터랙티브 호버 피드백**:
     - 마우스 오버 시 브랜드 블루(`#1971c2`) 배경과 흰색 텍스트, 은은한 블루 섀도우로 자연스럽게 전환되어 명확한 사용자 인터랙션 피드백 제공.

### 5.40 플레이리스트 드롭다운 재생 중 항목 라이브 이퀄라이저 탑재 (`YoutubeOffcanvas.js`, `YoutubeOffcanvas.module.css`)
- **도입 배경**:
  - 오프캔버스 플레이리스트 드롭다운 목록에서 현재 재생 중인 플레이리스트 항목이 단순히 배경색만 변경되어 있어, 라이브 재생 상태를 역동적이고 직관적으로 인지하기 어려움.
- **구현 내용**:
  1. **드롭다운 메뉴 재생 중 항목 이퀄라이저 연동**:
     - '전체', '미분류' 및 사용자 생성 플레이리스트 항목 중 현재 재생 중인 플레이리스트(`playingPlaylistId`) 항목 우측(곡 수 뱃지 앞)에 3단 라이브 이퀄라이저 애니메이션(`styles.equalizer`) 탑재.
     - 곡 재생 중(`isPlaying === true`)에는 3개의 바가 경쾌하게 바운스 애니메이션을 수행하고, 일시정지(`isPlaying === false`) 시에는 `.paused` 스타일로 고정/뮤트되어 플레이어의 실제 재생 상태를 실시간으로 정확하게 반영.
  2. **드롭다운 토글 버튼 연동**:
     - 현재 화면에 선택된 플레이리스트가 재생 중인 플레이리스트와 일치할 경우, 드롭다운 토글 버튼의 곡 수 뱃지 좌측에도 미니 이퀄라이저를 노출하여 드롭다운을 열지 않고도 현재 재생 플레이리스트 여부를 즉각 확인 가능.
  3. **스타일 동기화**:
     - `.playingItem` 및 `.playlistDropdownToggle` 내부의 이퀄라이저 바 색상을 브랜드 블루(`#1971c2`)로 통일하여 세련된 톤앤매너 완성.

### 5.41 플레이리스트 관리 모달 긴 이름 곡 수 뱃지 개행(줄바꿈) 현상 해결 (`PlaylistManageModal.js`)
- **원인 분석**:
  - 플레이리스트 항목 제목 영역에 `d-flex align-items-center flex-wrap gap-1`이 적용되어 있어, 플레이리스트 이름이 조금만 길어져도 곡 수 뱃지(`N곡`)가 다음 줄로 줄바꿈되어 이름, 뱃지, 공유코드가 3줄로 지저분하게 늘어지는 레이아웃 깨짐 현상 발생.
- **해결 방안**:
  1. **1행 고정 (`flex-nowrap`)**: 제목 컨테이너에서 `flex-wrap`을 제거하고 `flex-nowrap`을 적용하여 뱃지가 절대 다음 줄로 떨어지지 않도록 고정.
  2. **가변 축소 및 말줄임 처리**: 제목 `<span>`에 `minWidth: 0`과 `text-truncate`를 부여하고, 뱃지 `<span>`에는 `flex-shrink-0`을 지정하여 긴 이름은 부드럽게 말줄임표(`...`) 처리되고 뱃지는 온전한 형태를 항상 유지하도록 개선.
  3. **툴팁 제공**: 말줄임표 처리된 긴 이름도 마우스 오버 시 전체 이름을 확인할 수 있도록 `title={pl.name}` 속성 추가.

### 5.42 플레이리스트 관리 모달 디자인·타이포그래피·간격·UX 라이팅 전면 정제 (`PlaylistManageModal.js`, `PlaylistManageModal.module.css`)
- **문제점**:
  - 두 입력 폼의 버튼 스타일 불일치, 반복되는 이모지(`🎵`, `🔗`, `📥`)로 인한 조잡함 및 시선 분산.
  - 플레이리스트 항목에서 곡 수 뱃지가 제목 옆에 붙어 오른쪽 버튼과 충돌하거나 밀리는 현상.
  - 3개 액션 버튼(`공유`, `수정`, `삭제`)이 좁은 간격으로 다닥다닥 붙어 있어 답답함을 유발함.
  - Windows 네이티브 투박한 16px 스크롤바 노출 및 푸터의 칙칙한 차콜색 `[닫기]` 버튼(`btn-secondary`).
- **개선 내용**:
  1. **불필요한 이모지 완전 제거 및 텍스트 정제**:
     - 모달 타이틀(`플레이리스트 관리`), 플레이리스트 이름, 버튼(`공유`, `복사됨`, `가져오기`)에서 반복되던 이모지(`🎵`, `🔗`, `📥`)를 전면 제거하여 모던하고 정갈한 타이포그래피 완성.
     - `공유코드로 플레이리스트 가져오기 (복사 생성)` ➔ `공유코드로 플레이리스트 가져오기`로 직관화.
     - `내 플레이리스트 (7)`를 제목과 라운드 뱃지 `7개`로 분리.
  2. **1행(제목) & 2행(메타데이터) 시각 계층 완벽 분리**:
     - 1행: 플레이리스트 이름에 100% 가용폭을 부여하여 긴 제목도 버튼과 충돌 없이 넓고 여유롭게 표시.
     - 2행: `곡 수`와 `공유코드 칩`([`.metaRow`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/PlaylistManageModal.module.css#L112-L121))을 일원화하여 정돈된 레이아웃 제공.
  3. **버튼 간격 확장 및 모던 소프트 틴트 버튼 ([`PlaylistManageModal.module.css`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/PlaylistManageModal.module.css))**:
     - 액션 버튼 컨테이너에 [`.actionGroup`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/PlaylistManageModal.module.css#L134-L139)(`gap: 8px;`)을 적용하고 버튼 패딩을 `4px 10px`로 키워 답답함 해소.
     - 텍스트와 버튼 사이에 `me-3`(16px) 여백을 두어 쾌적한 분리감 확보.
     - 항목 액션: 소프트 스카이블루 `[공유]`, 소프트 민트 `[복사됨]`, 소프트 그레이 `[수정]`, 소프트 코랄레드 `[삭제]`로 시각적 피로도 제거.
     - 푸터 닫기: 칙칙한 다크 그레이 대신 깔끔한 소프트 라이트 그레이 버튼 ([`.closeBtn`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/PlaylistManageModal.module.css#L198-L211))으로 개편.
### 5.43 플레이리스트 가져오기 프론트엔드 사전 검증(Pre-Validation) 체계 구축 (`PlaylistManageModal.js`)
- **도입 배경**:
  - 공유코드 입력 시 1글자나 한글, 유효하지 않은 문자열을 입력해도 API 요청이 그대로 서버로 전송되어, 불필요한 백엔드 DB 조회 및 에러 알림창이 발생하는 문제 개선.
- **구현 내용**:
  1. **실시간 입력 제어 (`maxLength={8}`, 공백 제거 및 대문자화)**:
     - 입력창에 `maxLength={8}` 및 `autoComplete="off"`를 적용하고, 입력 시 공백을 자동 제거 및 대문자로 통일.
  2. **가져오기 버튼 활성화 제어 (`isValidImportCode`)**:
     - 정규식 `/^[A-Z0-9]{8}$/` 검증을 통과한 정확한 8자리 영문/숫자 코드가 입력되었을 때만 [가져오기] 버튼이 활성화되도록 제어하여 비정상 요청 원천 차단.
  3. **제출 시 사전 형식 검증 및 본인 플레이리스트 복제 허용**:
     - 8자리 영문/숫자 정규식 형식 검사 실패 시 친절한 안내 알림(`8자리 영문과 숫자로 구성되어야 합니다`) 노출.
     - 본인 소유 플레이리스트의 공유코드 입력 시에도 백엔드의 복사 생성(`import_playlist`) 로직을 통해 독립된 새 고유 코드를 가진 복제본 플레이리스트 생성을 정상 허용.

### 5.44 모달 인풋 텍스트 드래그 후 모달 바깥 마우스업(MouseUp) 시 모달 닫힘 버그 수정 (`PlaylistManageModal.js`, `VideoPlaylistTagModal.js`)
- **원인 분석**:
  - React-Bootstrap의 `<Modal>`은 모달 내부에서 시작된 드래그 선택이 모달 바깥(백드롭)에서 끝나더라도 모달이 닫히지 않도록, 모달 다이얼로그의 `mousedown` 이벤트(`handleDialogMouseDown`)를 감지하여 `waitingForMouseUpRef` 플래그를 세우고 백드롭 클릭을 무시(`ignoreBackdropClickRef = true`)하는 자체 방어 메커니즘을 내장하고 있음.
  - 그러나 과거 모바일 포커스 이슈 대응 시 모달 내 `<Form.Control>` 인풋들에 임의로 적용되었던 `onMouseDown={(e) => e.stopPropagation()}` 코드가 `mousedown` 이벤트의 버블링을 원천 차단함.
  - 그 결과 인풋 안에서 텍스트를 드래그하여 선택한 후 마우스 커서가 모달 영역 바깥으로 나가서 마우스를 뗄 경우(`mouseup`), React-Bootstrap이 드래그 시작점을 감지하지 못해 일반 백드롭 클릭으로 오인하고 `onHide()`를 호출하여 모달이 꺼지던 버그 발생.
- **해결 내용**:
  - `PlaylistManageModal.js`(플레이리스트 이름 인풋, 공유코드 인풋, 인라인 수정 인풋) 및 `VideoPlaylistTagModal.js`(신규 추가 인풋)의 `<Form.Control>`에서 불필요한 `onMouseDown={(e) => e.stopPropagation()}`을 전면 제거.
  - React-Bootstrap의 정상적인 `mousedown ➔ mouseup` 이벤트 감지 흐름을 복원하여, 인풋 내에서 텍스트를 드래그한 채 마우스를 모달 바깥에서 떼어도 모달이 닫히지 않고 안정적으로 선택 상태를 유지하도록 수정.

### 5.45 창 너비 축소 시 유튜브 오프캔버스 드래그 핸들 사라짐 현상 수정 (`YoutubeOffcanvas.js`)
- **원인 분석**:
  - 기존에 오프캔버스 확장 및 드래그 핸들 표시 조건(`canExpand`)의 최소 창 너비(`MIN_WINDOW_WIDTH_FOR_EXPAND`)가 `1000px`로 하드코딩되어 있었음.
  - 이로 인해 데스크톱/노트북에서 브라우저 창 너비를 1000px 미만(예: 분할 화면 960px, 창 축소 시 995px 등)으로 줄이면, 오프캔버스를 확장할 가용 폭(최대 955px 등)이 충분함에도 드래그 핸들이 화면에서 갑자기 사라지는 문제 발생.
- **해결 내용**:
  - 최소 가용 너비 기준을 `1000px`에서 태블릿/모바일 분기점인 `576px`로 완화(`MIN_WINDOW_WIDTH_FOR_EXPAND = 576`).
  - 브라우저 창 리사이즈 감지(`handleResize`) 시 가용 최대 너비(`currentMaxWidth = Math.min(1000, currentWinWidth - 40)`)에 맞춰 오프캔버스 너비를 부드럽게 동적 축소·클램핑하도록 개선.
  - 핸들 탭 토글 및 `isExpanded` 감지 기준을 `prev > DEFAULT_WIDTH`(500px)로 유연화하여, 576px 이상의 모든 반응형 환경(995px, 960px 분할화면, 태블릿 등)에서 드래그 핸들이 항상 표시되고 자연스럽게 리사이징 및 토글되도록 완성.

### 5.46 유튜브 오프캔버스 재생 중 표시 중복 제거 및 이퀄라이저 싱크 동기화 (`YoutubeOffcanvas.js`, `YoutubeOffcanvas.module.css`)
- **원인 분석**:
  1. **표시 중복**: 상단 헤더 뱃지(`[ ılı 재생 중 | 플레이리스트명 ]`), 드롭다운 토글 및 드롭다운 메뉴 아이템, 그리고 다른 플레이리스트 탐색 시 노출되는 하단 복귀 배너(`[ ılı 재생 중 | 플레이리스트명 ] [ 재생 목록 보기 ➔ ]`)까지 동일한 "재생 중" 문구와 뱃지가 동시에 3~4군데에서 중복 노출되어 시각적 피로도와 조잡함을 유발함.
  2. **이퀄라이저 싱크 불일치**:
     - 하단 복귀 배너의 이퀄라이저에 `${!isPlaying ? styles.paused : ""}` 클래스가 누락되어, 영상이 일시정지(`PAUSED`) 상태임에도 하단 배너의 이퀄라이저 바는 계속해서 춤을 추고 상단 뱃지는 멈춰 있는 심각한 싱크 불일치 발생.
     - 곡이 아직 한 번도 재생되지 않은 초기 상태(`!hasStarted`)에서도 "전체" 플레이리스트가 "재생 중"으로 표시되는 논리적 오류 존재.
     - 영상이 일시정지 상태임에도 문구가 여전히 `재생 중`으로 고정 노출됨.
- **해결 내용**:
  1. **상단 헤더 중복 뱃지 완전 제거**:
     - 오프캔버스 상단 헤더에서 불필요하게 자리를 차지하고 중복을 유발하던 `playingBadge`를 완전 제거하여, 타이틀(`유튜브 재생 목록`)과 닫기 버튼만 깔끔하게 남김.
  2. **이퀄라이저 싱크 및 재생 상태 100% 동기화**:
     - 하단 복귀 배너의 이퀄라이저에 `!isPlaying ? styles.paused : ""`를 적용하고, `.paused .equalizerBar`에 `!important`를 부여하여 영상 일시정지 시 모든 이퀄라이저 바가 즉각 동작을 멈추고 차분한 그레이 정지 상태로 동기화되도록 수정.
     - `onStateChange`에서 재생(`PLAYING`) 외 모든 상태(일시정지, 버퍼링, 종료 등)에서 `isPlaying`을 정확히 `false`로 리셋.
  3. **실제 재생 시작 여부(`hasStarted`) 연동**:
     - 첫 재생이 시작되기 전에는 어떤 플레이리스트에도 "재생 중" 표시나 이퀄라이저가 노출되지 않도록 `hasStarted` 가드 적용.
  4. **동적 상태 문구 (`재생 중` ↔ `일시정지`)**:
     - 하단 복귀 배너의 상태 라벨을 `isPlaying ? "재생 중" : "일시정지"`로 동적 분기하여 실제 음악 재생 상태와 시각적 텍스트가 완벽하게 일치하도록 개선.

### 5.47 기능 추가 내역(Release Note) 모달 최신 업데이트 반영 (`FeatureHistoryModal.js`)
- **내용**: 최근 구현된 대규모 유튜브 플레이리스트 신규 기능 및 기존 핸들 개선 내역을 '유튜브 연속 재생 플레이어' 카테고리에 최신 날짜(`2026.09.14`)로 일괄 반영.
- **사용자 친화적 서술 및 태깅 정합성 원칙**:
  - 개발 과정의 내부 수정 내역(이퀄라이저 싱크, 관리창 UI 다듬기 등)을 '개선'으로 잘못 분리 표기하던 문제를 바로잡고, 이번 릴리즈로 처음 탑재되는 플레이리스트 기능들은 모두 **`[추가]`**로 통합 구성.
  - 기존에 이미 존재하던 기능(8.18 대화면 도트 핸들 리사이저)에 대한 고도화 항목만 **`[개선]`**으로 명확히 구분.
- **정제된 릴리즈 항목**:
  1. `[추가] 플레이리스트 생성 및 한 곡 다중 담기` (원하는 이름으로 재생 목록을 생성하고 영상마다 복수 선택 분류)
  2. `[추가] 8자리 코드로 플레이리스트 공유 및 가져오기` (고유 공유코드로 친구와 공유 및 원클릭 복사 보관)
  3. `[추가] 끊김 없는 플레이리스트 둘러보기 & 라이브 이퀄라이저` (다른 목록을 둘러보아도 음악 정지 없이 재생 유지 및 실시간 이퀄라이저 표시)
  4. `[개선] 재생 목록 창 크기 조절 편의성 향상` (기존 도트 핸들이 좁은 창이나 분할 화면에서도 사라지지 않도록 개선)
- **동적 정렬 및 NEW 뱃지**: 최신 업데이트 일자(`2026.09.14`)를 기준으로 유튜브 카테고리가 모달 최상단에 자동 정렬 및 펼쳐지며, 2주 이내 `NEW` 뱃지가 적용됨.

### 5.48 업데이트 내역 과도한 길이 절충안: 최근 내역 기본 표시 및 '이전 업데이트 더보기' 토글 시스템 (`FeatureHistoryModal.js`)
- **도입 배경**:
  - 기존 500px 고정 시에는 과거 로그가 잘리는 문제("이전로그 확인이 안됨")가 있었으나, 높이 상한을 단순 해제할 경우 방대한 이전 업데이트 항목이 한꺼번에 길게 펼쳐져 하위 카테고리(검색, 웹링크, 게시판 등)를 가리고 과도한 스크롤 피로도("너무 긴것")를 유발함.
- **절충안(Compromise) 구현 내용**:
  1. **최신 릴리즈 내역 우선 기본 표시 (4개)**:
     - 카테고리 오픈 시 최신 릴리즈 일자의 4개 핵심 항목들(`2026.09.14`)만 기본 표시하여 약 300px의 콤팩트하고 시원한 높이 유지.
     - 모달 오픈 즉시 다른 하위 카테고리들의 존재를 한눈에 파악할 수 있는 시각적 개방감 확보.
  2. **'이전 업데이트 (N개) 더보기' / '접기' 원클릭 토글 버튼**:
     - 기본 표시 개수를 초과하는 이전 내역이 존재할 경우, 하단에 모던 소프트 틴트 버튼([`▼ 이전 업데이트 7개 더보기`]) 제공.
     - 클릭 시 이전 업데이트 항목들(`2026.08.18` ~ `2022.11.09`)이 아래로 부드럽게 펼쳐지며, 재클릭 시 [`▲ 이전 업데이트 접기`]로 원클릭 콤팩트 축소 지원.
  3. **반응형 아코디언 애니메이션 튜닝**:
     - 기본 상태에서는 `maxHeight: "800px"`로 동작하여 카테고리 여닫힘 시 딜레이 없이 부드러운 트랜지션을 제공하고, 더보기 활성화 시 `maxHeight: "3000px"`로 유연하게 전환되어 잘림 없는 열람 보장.

### 5.49 라이브 게시판(2018~2022) 공지 기반 기능 최초 도입 일자 팩트 동기화 (`FeatureHistoryModal.js`)
- **배경**:
  - 기존 레거시 기능들의 일자가 React 전면 개편 초기 Git 커밋 일자(`2022.11`)로 일괄 지정되어 있었으나, 실제 라이브 서버(`https://bokk.jiokkae.com/messageboard/`) 게시판의 과거 작성글들을 전수 조회하여 각 기능이 최초 도입/공지된 정확한 실제 일자로 전면 동기화함.
- **실제 게시판 글 기반 수정 내역**:
  1. **내 웹링크 관리**:
     - `2018.05.04` (게시글 #353 "커스텀 웹링크 기능이 추가대엇음니다") 최초 도입일 반영.
     - `2022.11.15` (게시글 #690 "웹링크 수정 기능이 추가 대었읍니다") 웹링크 수정 기능 추가 일자 반영.
  2. **유튜브 연속 재생 플레이어**:
     - `2018.12.28` (게시글 #436 "유튜부 플레이어가 추가대엇읍니다......") 유튜브 플레이어 최초 오픈일 반영.
  3. **포털 & 전적 통합 검색**:
     - `2018.11.21` (게시글 #384 "구글 검색 추가대엇읍니다....") 구글 검색 연동 일자 반영.
  4. **자유 게시판 & 소통**:
     - `2018.05.06` (게시글 #1 "첫글입니다", #161 "게시판에 삭제 버튼이 추가되었습니다") 게시판 서비스 개시일 반영.
     - `2022.12.15` (게시글 #706 "게시판에 여러가지 추가대었읍니다…") WebP 이미지 및 유튜브 영상 미디어 첨부 에디터 강화 일자 반영.

### 5.50 빈 플레이리스트 안내문 및 곡 담기 이동 버튼 고도화 (`YoutubeOffcanvas.js`, `Playlist.js`)
- **문제점**:
  - 오프캔버스에서 빈 플레이리스트 선택 시 "영상 우측의 🏷️ 태그 버튼으로 이 플레이리스트에 추가해보세요!"라는 레거시 안내 문구가 노출되었음.
  - 리뉴얼된 UI 명칭(`+ 담기`)과 불일치할 뿐만 아니라, 빈 목록 화면에는 클릭할 영상 행 자체가 존재하지 않아 사용자가 무엇을 눌러야 할지 알 수 없는 UX 막다른 골목(Dead End) 발생.
  - 플레이리스트 관리 페이지(`Playlist.js`)에서도 여전히 과거의 `🏷️ 태그` 버튼 텍스트가 남아 있어 명칭 불일치 존재.
- **개선 내용**:
  1. **상황별 친절한 안내 문구 세분화 ([`YoutubeOffcanvas.js`](file:///d:/GitHub/bokk/src/components/Header/SearchSelector/YoutubeOffcanvas/YoutubeOffcanvas.js))**:
     - 전체 등록 영상이 0개일 때: `"재생 목록에 등록된 영상이 없습니다."` / `"상단 입력창에 유튜브 영상 주소를 입력하여 추가해보세요!"`
     - 미분류 영상이 0개일 때: `"미분류 영상이 없습니다."` / `"모든 영상이 플레이리스트에 정리되어 있습니다."`
     - 특정 플레이리스트 영상이 0개일 때: `"이 플레이리스트에 담긴 영상이 없습니다."` / `"전체 목록에서 영상 우측의 '+ 담기' 버튼으로 이 플레이리스트에 곡을 담아보세요!"`
  2. **원클릭 이동 액션 버튼 제공**:
     - 빈 플레이리스트 화면 하단에 [`[ 전체 목록에서 곡 담으러 가기 ➔ ]`] 버튼을 제공하여, 클릭 즉시 `ALL`(전체 목록)으로 전환되어 사용자가 번거롭게 상단 드롭다운을 다시 조작하지 않고도 손쉽게 곡을 담을 수 있도록 안내.
  3. **관리 페이지 명칭 및 스타일 일원화 ([`Playlist.js`](file:///d:/GitHub/bokk/src/routers/manage/Playlist.js))**:
     - `🏷️ 태그` 표기를 오프캔버스와 동일하게 `담김 N` / `+ 담기` 및 소프트 스카이블루 버튼 스타일로 통일.

### 5.51 유튜브 썸네일 404 더미 이미지 감지 및 RESOLUTION.MQ 규격 최적화 (`YoutubeVideoItem.js`, `Playlist.js`, `ThumbnailModal.js`)
- **원인 분석**:
  - 유튜브 이미지 서버(`img.youtube.com`)는 고화질(`maxresdefault.jpg`, 1280x720)이 지원되지 않는 영상(SD 해상도나 과거 업로드 영상, 예: "Running in the 90's")에 대해 HTTP 404를 반환할 때, 응답 본문에 **120x90 크기의 회색 '...' 더미 이미지(JPEG)**를 실어 보냄.
  - 브라우저(`<img>`)는 유효한 JPEG 이미지가 수신되었으므로 이를 에러(`onError`)로 인식하지 않고 정상 로드(`onLoad`)된 것으로 처리함.
  - 이로 인해 기존 `onError` 폴백 로직이 동작하지 않고 회색 점선 더미 썸네일이 화면에 영구 노출되는 버그 발생.
- **해결 내용**:
  1. **목록 썸네일 규격 최적화 (`RESOLUTION.MQ` 320x180)**:
     - 목록의 썸네일 표시 영역은 104x58px(16:9 비율)이므로, 320x180 해상도의 `RESOLUTION.MQ`는 약 3배수(3x Retina)에 달해 극도로 선명하며 레터박스(상하 검은 띠) 없이 꽉 참.
     - 모든 유튜브 영상에서 100% 지원되므로 불필요한 404 지연과 트래픽 낭비(1280px 대비 30배 가벼움)를 원천 차단.
  2. **120x90 더미 이미지 감지 및 자동 폴백 (`naturalWidth === 120 && naturalHeight === 90`)**:
     - `onLoad` 이벤트 핸들러에서 유튜브의 120x90 더미 이미지를 정밀 감지하여, 즉시 유효한 하위 해상도(`hq` / `default`)로 자동 교체되도록 안전장치 구축.
     - `YoutubeVideoItem.js`, `Playlist.js`, `ThumbnailModal.js` 전반에 일괄 적용 완료.

### 5.52 웹링크 그룹(Weblink Group) 관리 시스템 구축 (`Home.js`, `MyWeblink.js`, `WeblinkGroupManageModal.js`, `WeblinkForm.js`, `AddWeblinkModal.js`, `UpdateWeblinkModal.js`, `querys.js`)
- **도입 배경**: 사용자가 등록한 웹링크들을 주제별(업무, 개발, 취미, 쇼핑 등)로 분류하여 편리하게 접근하고 관리할 수 있도록 웹링크 그룹 시스템 구축.
- **구현 내용**:
  1. **웹링크 그룹 관리 모달 신규 탑재 (`WeblinkGroupManageModal.js`)**:
     - 새 그룹 생성 인풋 (엔터 키 지원 및 유효성 검사).
     - 실시간 그룹 목록 및 소속 링크 개수 배지 표시.
     - 인라인 그룹 이름 수정 (저장/취소) 및 그룹 삭제 시 안전 보존 다이얼로그 (소속 링크는 미분류로 안전 전환).
     - `☰` 핸들을 통한 직관적인 실시간 드래그 앤 드롭 그룹 순서 변경 연동.
  2. **관리 페이지(`MyWeblink.js`) 그룹별 섹션 분리 및 정렬**:
     - 상단에 `[⚙️ 그룹 관리]` 버튼 배치.
     - 그룹별로 구분된 헤더 섹션과 각 그룹 내 전용 `+` 추가 버튼 및 그룹 내 드래그 앤 드롭 정렬 지원.
     - 하단에 `🔗 미분류` 섹션을 분리 제공하여 그룹 미지정 링크도 안전하고 체계적으로 관리.
  3. **메인 대시보드(`Home.js`, `Home.module.css`) 그룹별 행 분리 렌더링**:
     - 그룹이 존재하는 경우 메인 화면에서 각 그룹별로 행(Row)을 나누고, 앞머리에 모던한 스타일의 배지(`📁 [그룹명]`, `🔗 미분류`)를 노출.
     - 각 그룹 행 끝에 해당 그룹으로 즉시 추가할 수 있는 `+` 버튼 제공.
     - 그룹이 없거나 비로그인 상태일 때는 기존의 단일 행 디자인을 그대로 유지하여 100% 하위 호환 보장.
  4. **웹링크 폼 및 모달 그룹 지정 지원 (`WeblinkForm.js`, `AddWeblinkModal.js`, `UpdateWeblinkModal.js`)**:
     - '소속 그룹' 드롭다운(`<Form.Select>`)을 추가하여 링크 등록/수정 시 원하는 그룹 선택 또는 그룹 간 자유로운 이동 지원.

### 5.53 웹링크 드래그 앤 드롭을 통한 실시간 그룹 간 이동 및 빈 그룹 드롭 지원 (`MyWeblink.js`, `querys.js`, `lib/bokk/db.php`, `public/api/schema.graphql`, `public/api/index.php`)
- **도입 배경**: 사용자가 웹링크 관리 화면에서 순서 변경용 핸들(`☰`)을 잡고 드래그하여 다른 그룹으로 손쉽게 웹링크의 소속 그룹을 변경할 수 있도록 사용자 경험 고도화.
- **구현 내용**:
  1. **백엔드 그룹 변경 전용 Mutation 구축 (`setWeblinkGroup`)**:
     - `set_weblink_group(int $weblink_id, ?int $group_id, string $user_id): bool` 메서드 및 `setWeblinkGroup(weblinkId: Int!, groupId: Int): Boolean!` GraphQL API 구축.
  2. **크로스 그룹(Cross-Group) 실시간 드래그 앤 드롭 연동 (`MyWeblink.js`)**:
     - 드래그 중 다른 그룹의 특정 위치로 진입 시 즉시 해당 그룹으로 아이템이 이동하는 실시간 미리보기 반응형 렌더링.
     - 드롭(`onDragEnd`) 완료 시 원래 소속 그룹과 비교하여 그룹 변경 시 `setWeblinkGroup`을 자동 호출하고, 전체 순서 `reorderWeblinks`를 연계 실행.
  3. **빈 그룹(Empty Group) 드롭 존(Drop Zone) 지원**:
     - 그룹 내 웹링크가 0개인 빈 그룹 영역에도 드래그 진입 시 `"⬇ 여기에 드롭하여 이 그룹으로 이동"` 점선 배지 및 액티브 하이라이트가 활성화되어 빈 그룹으로의 이동도 완벽 지원.

### 5.54 웹링크 그룹 컨테이너(Span) 내 상단 헤더라인 및 모두 열기(새 탭 / Shift+클릭 새 창) 기능 고도화 (`Home.js`, `Home.module.css`)
- **스팬 래핑 컨테이너 및 상단 헤더라인 구조화**: 각 그룹을 감싸는 카드형 스팬 컨테이너(`.groupContainer`, `.groupContainerUntagged`) 내부에 상단 헤더라인(`.groupHeaderLine`)을 배치하여, 상단 헤더라인에 그룹명 뱃지(`groupBadge`: `#495057`, `#f1f3f5`)와 `[↗ 모두 열기]` 버튼을 깔끔하게 배치하고, 하단 본문 영역(`.groupContent`)에 웹링크 버튼 및 `+` 추가 버튼을 정돈.
- **일관된 그룹핑**: 빌트인 웹링크 또한 `bokk` 그룹 컨테이너로 통일감 있게 감싸고, 이모지(📁, 📎)는 제거하여 깔끔하고 세련된 대시보드 UI 연출.
- **그룹별 '모두 열기' 지원**: 각 그룹 상단 헤더라인에 `[↗ 모두 열기]` 버튼을 제공하여 소속된 모든 링크를 한 번에 열람 가능.
- **Shift 키 인터랙션 (새 탭 vs 새 창 분기)**:
  - 기본 클릭: 현재 창에서 모든 링크를 새 탭으로 오픈.
  - Shift + 클릭: `<a>` 태그의 브라우저 네이티브 동작을 활용하여 탭 바가 온전히 유지되는 표준 새 브라우저 창으로 전체 링크 일괄 오픈. 실시간 Shift 키 감지를 통해 버튼 텍스트가 '⧉ 새 창으로 모두 열기'로 동적 전환 및 보라색 하이라이트 피드백 제공.
- **반응형 및 빈 미분류 자동 숨김**: 미분류 링크가 없을 때는 불필요한 빈 미분류 컨테이너가 노출되지 않도록 스마트 렌더링 적용.

### 5.55 그룹 관리에 빌트인 그룹(`bokk`) 순서 변경(드래그 앤 드롭) 지원 (`WeblinkGroupManageModal.js`, `Home.js`, `MyWeblink.js`, `WeblinkForm.js`, `lib/bokk/db.php`)
- **빌트인 그룹(id: 0) 가상 매핑 및 순서 저장**: 백엔드 `get_user_weblink_groups`에서 기본 빌트인 그룹(`bokk`, `id: 0`)을 유저 그룹 목록에 통합 반환하고, `reorder_weblink_groups`에서 `id: 0`의 순서를 사용자의 `user.config` (`builtinGroupSeq`)에 영구 보존.
- **그룹 관리 모달(`WeblinkGroupManageModal.js`) 연동**: 빌트인 그룹(`bokk`)도 `☰` 핸들을 잡고 원하는 위치로 자유롭게 드래그하여 순서를 변경 가능. '기본' 배지 표시 및 수정/삭제 버튼은 비활성화하여 기본 그룹 안전성 보장.
- **메인 화면(`Home.js`) 동적 순서 렌더링**: 그룹 목록의 정렬 순서에 따라 `bokk` 그룹이 사용자 그룹 사이, 앞, 뒤 등 사용자가 배치한 정확한 위치에 카드 형태로 렌더링.
- **소속 그룹 격리**: 커스텀 링크 등록/수정 폼(`WeblinkForm.js`) 및 관리 페이지(`MyWeblink.js`)에서는 `id > 0` 필터링을 통해 일반 링크가 빌트인 그룹에 오지정되지 않도록 완벽 격리.

### 5.56 미분류 그룹(`미분류`, id: -1) 순서 변경(드래그 앤 드롭) 지원 (`WeblinkGroupManageModal.js`, `Home.js`, `MyWeblink.js`, `WeblinkForm.js`, `lib/bokk/db.php`)
- **미분류 그룹(id: -1) 가상화 및 순서 저장**: 백엔드 `get_user_weblink_groups`에서 미분류 그룹(`미분류`, `id: -1`)을 유저 그룹 목록에 자동 통합 반환하고, `reorder_weblink_groups`에서 `id: -1`의 순서를 사용자의 `user.config` (`untaggedGroupSeq`)에 영구 보존.
- **그룹 관리 모달(`WeblinkGroupManageModal.js`) 연동**: 미분류 그룹도 `☰` 핸들을 잡고 원하는 위치로 자유롭게 드래그하여 순서 변경 가능. '미분류' 배지 및 미분류 링크 실시간 개수 카운트 표시, 수정/삭제 버튼은 비활성화(시스템 그룹).
- **메인 화면(`Home.js`) 동적 순서 렌더링**: 사용자가 배치한 순서(`seq`)에 따라 `미분류` 그룹이 `bokk` 및 사용자 그룹 사이 원하는 위치에 렌더링되며, 미분류 링크가 0개일 때는 자동으로 숨김 처리.
- **소속 그룹 격리**: 커스텀 링크 등록/수정 폼(`WeblinkForm.js`) 및 관리 페이지(`MyWeblink.js`)에서는 `id > 0` 필터링을 적용하여 실제 사용자 커스텀 그룹만 선택되도록 보호.

### 5.57 메인 대시보드 상단 툴바 및 인플레이스(In-Place) 드래그 순서/그룹 변경 탑재 (`Home.js`, `Home.module.css`)
- **웹링크 구역 상단 툴바 (`.weblinkToolbar`)**:
  - 웹링크 영역 상단에 여백(`margin-top: 1.25rem`)을 둔 전용 라인을 배치하고 우측 정렬로 액션 버튼 배치.
  - **[⇄ 순서·그룹 변경] 버튼**: 클릭 시 메인 화면에서 즉시 드래그 모드가 활성화(`✓ 변경 완료` 토글).
  - **[⚙️ 웹링크 관리] 버튼**: 웹링크 상세 관리 페이지(`/manage/myWeblink/`)로 바로 이동할 수 있는 바로가기 버튼 제공.
- **메인 화면 인플레이스 드래그 앤 드롭 (`Home.js`)**:
  - **웹링크 순서 및 그룹 변경**: 드래그 모드 활성화 시 웹링크를 드래그하여 같은 그룹 내 순서 변경은 물론, 다른 그룹으로 드래그하여 소속 그룹을 즉각 변경 가능.
  - **빈 그룹 및 미분류 드롭 존**: 링크가 비어 있는 그룹 또는 미분류 영역에도 드롭 존(`⬇ 여기에 드롭하여 이동`)을 제공하여 손쉽게 이동 가능.
  - **그룹 자체 순서 변경**: 각 그룹 헤더의 핸들(`☰`)을 드래그하여 메인 화면에서 그룹 간 순서도 즉시 재배치 가능.
### 5.58 웹링크 관리 및 툴바 비로그인 접근 제어 (`Home.js`, `MyWeblink.js`)
- **메인 화면 툴바 숨김 (`Home.js`)**: 비로그인 방문자에게는 웹링크 구역 상단의 `[⇄ 순서·그룹 변경]` 및 `[⚙️ 웹링크 관리]` 툴바가 노출되지 않도록 `{isLogin && ...}` 조건부 렌더링 적용.
- **관리 페이지 인증 가드 및 리다이렉트 (`MyWeblink.js`)**:
  - `useQuery(ME)` 상태를 확인하여 비로그인 사용자가 `/manage/myWeblink/` URL로 직접 접근할 경우 로그인 페이지(`/signin/`)로 자동 리다이렉트(`navigate("/signin/")`).
  - 컴포넌트 내 모든 React Hook(`useCallback`, `useEffect` 등)이 조건 없이 호출되도록 최상단에 Hook들을 완벽히 배치하여 React Hook 규칙(`react-hooks/rules-of-hooks`) 준수.
### 5.59 로그인 직후 웹링크 그룹 캐시 동기화 및 그룹 카드 렌더링 정상화 (`querys.js`, `Home.js`)
- **원인 분석**:
  1. `QUERIES_AFFECTED_BY_SIGN` 상수에 `MY_WEBLINK_GROUPS` 쿼리가 누락되어 있어, 로그인 완료 직후 Apollo Client가 `ME`와 `OWN_WEBLINKS`만 갱신하고 `MY_WEBLINK_GROUPS`를 재호출하지 않아 로그인 전의 빈 배열(`[]`) 캐시가 그대로 유지됨.
  2. `Home.js`에서 `groups.length === 0`일 때 레거시 미그룹(플랫) UI와 상단 빌트인 카드가 분리 렌더링되도록 분기되어 있어, 로그인 직후 빌트인 카드 아래에 그룹 틀 없이 원본 링크 버튼들이 흩뿌려지는 레이아웃 깨짐 현상 발생.
- **해결 방안**:
  1. **로그인/로그아웃 재조회 대상 통합 (`querys.js`)**: `QUERIES_AFFECTED_BY_SIGN` 배열에 `{ query: MY_WEBLINK_GROUPS }`를 추가하여 로그인/회원가입/로그아웃 시 항상 최신 웹링크 그룹 목록이 동기화되도록 수정.
  2. **그룹 폴백 및 렌더링 구조 개편 (`Home.js`)**:
     - `displayGroups`: 그룹 로딩 시점에도 기본 시스템 그룹(`bokk`: 0, `미분류`: -1)을 기본값으로 보장하여 빈 배열로 인한 레이아웃 깨짐을 원천 방지.
     - 로그인 사용자는 항상 일관된 그룹 카드 뷰(`displayGroups.map`)로 렌더링되도록 정리하고, 비로그인 방문자 전용 빌트인 뷰와 완벽하게 분리.
### 5.60 모바일 및 소형 터치 디바이스 '모두 열기' 자동 숨김 대응 (`Home.module.css`)
- **배경 및 필요성**:
  - 모바일 환경에서 '모두 열기' 터치 시 10~20개 이상의 탭이 백그라운드에 대량으로 한꺼번에 열리며 모바일 브라우저 랙(버벅임), 앱 튕김 또는 브라우저 자체 팝업 차단 경고가 발생하여 사용자 경험 저해.
  - 모바일 가상 키보드 환경에서는 Shift 키 조합('새 창으로 모두 열기')이 불가능하며, 좁은 헤더 영역에서 오터치(미스터치) 위험이 큼.
- **개선 내용**:
  - CSS 미디어 쿼리(`@media (max-width: 768px), (hover: none) and (pointer: coarse) and (max-width: 1024px)`)를 적용하여 모바일 화면 및 소형 터치 디바이스에서 `.openAllBtn` 및 `.openAllBtnShift`를 자동으로 숨김(`display: none !important;`) 처리.
  - PC 데스크톱 환경에서는 기존의 강력한 '새 탭/새 창 모두 열기' 기능이 유지되며, 모바일 화면에서는 깔끔하고 컴팩트한 그룹 헤더 배지만 노출되도록 최적화.
